{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"varTrack","text":"<p>GitOps-native configuration sync. Push a file to Git \u2014 varTrack writes it to your datastores automatically.</p> <p> </p>"},{"location":"#what-it-does","title":"What it does","text":"<p>You commit a config file. varTrack syncs it everywhere.</p> <pre><code># configs/app.yaml \u2014 lives in your Git repo\ndatabase_host: mongo.prod.internal\nfeature_flag_dark_mode: true\nmax_connections: 50\n</code></pre> <p>Push to <code>main</code> \u2192 varTrack automatically writes these values to every configured sink:</p> <pre><code>MongoDB     \u2192  collection \"production-config\"\nRedis       \u2192  hash       \"production:cfg\"\nZooKeeper   \u2192  znodes     /acme/production/\nS3          \u2192  key prefix acme/production/\n</code></pre> <p>If someone edits MongoDB directly and changes <code>max_connections</code> to <code>5</code>, the watcher detects it and restores <code>50</code> automatically \u2014 no manual intervention needed.</p>"},{"location":"#why-vartrack","title":"Why varTrack?","text":"<p>Tools like ArgoCD and FluxCD are excellent at deploying Kubernetes manifests from Git. They don't solve a different problem: syncing runtime configuration values into the datastores your applications actually read \u2014 MongoDB documents, Redis hashes, ZooKeeper znodes, S3 objects, Linux <code>.env</code> files.</p> <p>varTrack fills that gap. It treats Git as the single source of truth for application configuration data \u2014 not Kubernetes resources.</p> ArgoCD / FluxCD varTrack Target Kubernetes manifests MongoDB, Redis, ZooKeeper, S3, ConfigMap, Helm, Vercel, Linux Trigger Git push Git push / webhook Drift detection Kubernetes state Any datasource Secret management Kubernetes secrets HashiCorp Vault integration Schema validation None CUE schema per tenant <p>They complement each other: use ArgoCD to deploy your services, use varTrack to keep their runtime config in sync.</p>"},{"location":"#key-features","title":"Key features","text":"<ul> <li> <p> 8 sink types</p> <p>MongoDB, Redis, ZooKeeper, S3, Kubernetes ConfigMap, Helm, Vercel, Linux server (SSH)</p> <p> See all sinks</p> </li> <li> <p> Secret management</p> <p><code>@secret()</code> annotations pull values from HashiCorp Vault at ETL time. Secrets never touch Git. Dry-run reports mask them as <code>***</code></p> <p> Schema annotations</p> </li> <li> <p> CUE schema validation</p> <p>Every push is validated against a per-tenant CUE schema before any write. Invalid configs are rejected with a detailed error \u2014 zero bad writes</p> <p> Schema annotations</p> </li> <li> <p> Drift detection</p> <p>Watcher polls each datasource and compares against the Git baseline. On drift, logs the delta and optionally restores correct state \u2014 automatically</p> <p> How drift detection works</p> </li> <li> <p> Multi-sink fan-out</p> <p>One push writes to multiple sinks in parallel. One rule set for all environments</p> <p> Destination template</p> </li> <li> <p> Any config format</p> <p>YAML, JSON, TOML, .env, INI, HCL, XML \u2014 format auto-detected from file extension</p> <p> Supported formats</p> </li> <li> <p> CLI \u2014 <code>vt</code></p> <p>Push any local file from terminal or CI/CD. Validate against schemas. Inspect tasks</p> <p> CLI reference</p> </li> <li> <p> Dry-run mode</p> <p>Simulate any sync and see exactly what would be written \u2014 without touching a datasource. Secret fields masked as <code>***</code></p> <p> Dry-run reference</p> </li> </ul>"},{"location":"#try-it-in-one-command","title":"Try it in one command","text":"<p>The fastest way to see varTrack in action is the end-to-end demo. It starts a local Git server, MongoDB, ZooKeeper, Redis, and all three varTrack services:</p> <pre><code>cd e2e\ndocker compose up\n</code></pre> <p>Push a config change to the local repo and watch it sync automatically.</p>"},{"location":"#how-it-works","title":"How it works","text":"<pre><code>flowchart LR\n    GIT[\"\ud83d\udcc1 Git repo\\n(GitHub / Gitea / GitLab)\"]\n    GW[\"gateway-service\\nGo \u00b7 :5657\"]\n    ORCH[\"orchestrator-service\\nPython \u00b7 :50051\\n\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\\nRust ETL core\"]\n    WATCH[\"watcher-service\\nGo \u00b7 :9091\"]\n\n    GIT --&gt;|\"webhook\\n(push / PR)\"| GW\n    GW --&gt;|\"gRPC\\nProcessWebhook\"| ORCH\n\n    ORCH --&gt;|write| MONGO[\"MongoDB\"]\n    ORCH --&gt;|write| REDIS[\"Redis\"]\n    ORCH --&gt;|write| ZK[\"ZooKeeper\"]\n    ORCH --&gt;|write| S3[\"S3 / MinIO\"]\n    ORCH --&gt;|write| OTHER[\"ConfigMap \u00b7 Helm\\nVercel \u00b7 Linux\"]\n\n    WATCH --&gt;|\"poll\\n(drift check)\"| MONGO\n    WATCH --&gt;|\"poll\"| REDIS\n    WATCH --&gt;|\"TriggerSync\\n(self-heal)\"| ORCH</code></pre> <ol> <li>You push a config file.</li> <li>gateway-service (Go) receives the webhook, verifies the HMAC signature, and forwards it via gRPC.</li> <li>orchestrator-service (Python) fetches the file from Git, validates it against a CUE schema, runs the Rust ETL core (diff, merge, prune), and writes to all configured sinks.</li> <li>watcher-service (Go) polls datasources on a configurable interval. On drift, it logs the delta and \u2014 if <code>self_heal: true</code> \u2014 calls the orchestrator to restore correct state.</li> </ol> <p>Why Rust inside the orchestrator?</p> <p>The ETL core (diff, flatten, merge, prune) is implemented in Rust and called from Python via FFI. This keeps the hot path allocation-free and handles multi-megabyte config payloads without GC pressure, while the Python layer handles I/O, Vault, and sink drivers.</p> <p> Full architecture overview</p>"},{"location":"getting-started/","title":"Getting Started","text":"<p>Get varTrack running locally in under 10 minutes.</p>"},{"location":"getting-started/#prerequisites","title":"Prerequisites","text":"<ul> <li>Go 1.21+</li> <li>Python 3.11+</li> <li>A Celery broker \u2014 Redis or MongoDB (configured via <code>celery_broker_datasource</code> in the bundle)</li> <li>At least one configured sink</li> <li><code>buf</code> CLI \u2014 for proto code generation</li> </ul>"},{"location":"getting-started/#option-a-e2e-demo-fastest","title":"Option A \u2014 E2E demo (fastest)","text":"<p>No manual setup needed. Starts everything in Docker:</p> <pre><code>git clone https://github.com/jarin-devoss/varTrack\ncd varTrack/e2e\ndocker compose up\n</code></pre> <p>This starts a local Git server, MongoDB, ZooKeeper, Redis, and all three varTrack services with a pre-configured bundle. Push any config change to the local repo and watch it sync.</p>"},{"location":"getting-started/#option-b-build-from-source","title":"Option B \u2014 Build from source","text":""},{"location":"getting-started/#1-clone-and-generate-proto-code","title":"1. Clone and generate proto code","text":"<pre><code>git clone https://github.com/jarin-devoss/varTrack\ncd varTrack\nbuf generate\n</code></pre>"},{"location":"getting-started/#2-write-configcue","title":"2. Write <code>config.cue</code>","text":"<p>All three services share one CUE bundle that declares your platforms, datasources, and rules.</p> <pre><code>bundle: {\n  platforms: [{\n    github: {\n      endpoint:        \"https://github.com\"\n      push_event_name: \"push\"\n      pr_event_name:   \"pull_request\"\n      secret:          \"my-webhook-secret\"\n    }\n  }]\n\n  datasources: [{\n    mongo: {\n      endpoint:   \"mongodb://localhost:27017\"\n      database:   \"vartrack\"\n      collection: \"variables\"\n    }\n  }]\n\n  rules: [{\n    platform:             \"github\"\n    datasource:           \"mongo\"\n    file_name:            \"configs/app.yaml\"   // file to watch in each repo\n    repositories:         [\"my-org/*\"]         // glob \u2014 which repos trigger this rule\n    destination_template: \"{env}-config\"       // collection name resolved at sync time\n    self_heal:            true\n    branch_map: {\n      main:    \"production\"\n      develop: \"staging\"\n    }\n  }]\n}\n</code></pre>"},{"location":"getting-started/#3-start-the-gateway-service","title":"3. Start the gateway-service","text":"<pre><code>APP_ENV=dev \\\nORCHESTRATOR_ADDR=localhost:50051 \\\nCONFIG_PATH=config.cue \\\ngo run ./gateway-service/cmd\n</code></pre> <p>Listens on <code>:5657</code> (webhooks) and <code>:9090</code> (metrics/health).</p>"},{"location":"getting-started/#4-start-the-orchestrator-service","title":"4. Start the orchestrator-service","text":"<pre><code>cd orchestrator-service\npip install -e .\n\n# API + gRPC server\nuvicorn main:app --host 0.0.0.0 --port 8000 &amp;\n\n# Celery worker (separate terminal)\ncelery -A app.worker.celery worker --queues=webhooks,sync -l info\n</code></pre>"},{"location":"getting-started/#5-start-the-watcher-service","title":"5. Start the watcher-service","text":"<pre><code>ORCHESTRATOR_ADDR=localhost:50051 \\\nCONFIG_PATH=config.cue \\\ngo run ./watcher-service/cmd\n</code></pre> <p>Polls configured datasources every 60 seconds by default.</p>"},{"location":"getting-started/#6-register-a-webhook-in-github","title":"6. Register a webhook in GitHub","text":"<p>Point your repository webhook to <code>http://your-server:5657/webhooks/mongo</code>:</p> Setting Value Payload URL <code>http://your-server:5657/webhooks/mongo</code> Content type <code>application/json</code> Secret Value of <code>secret</code> from <code>config.cue</code> Events Pushes + Pull requests <p>Every push to a matching repository now triggers a sync automatically.</p>"},{"location":"getting-started/#verify-it-works","title":"Verify it works","text":"<p>Push a change to a tracked repository, then check MongoDB:</p> <pre><code>mongosh vartrack --eval 'db[\"production-config\"].find().pretty()'\n</code></pre> <p>You should see your config values written as key-value documents.</p>"},{"location":"getting-started/#install-the-cli","title":"Install the CLI","text":"<p>The <code>vt</code> CLI lets you sync files directly without a Git push:</p> <pre><code>cd cli\ngo build -o vt ./cmd\nmv vt /usr/local/bin/vt\n\n# Authenticate\nvt login --server http://localhost:5657 --token eyJ...\n\n# Push a file manually\nvt sync --file configs/app.yaml --datasource mongo --env staging --wait\n</code></pre> <p> Full CLI reference</p>"},{"location":"getting-started/#next-steps","title":"Next steps","text":"<ul> <li> <p> Destination template</p> <p>Control exactly where data lands using <code>{env}</code> and <code>{tenant}</code> placeholders</p> </li> <li> <p> Environment resolution</p> <p>Map branch names, PR numbers, and tags to environment names</p> </li> <li> <p> All sink types</p> <p>Configure MongoDB, Redis, ZooKeeper, S3, and more</p> </li> <li> <p> Drift detection</p> <p>Understand how the watcher detects and heals config drift</p> </li> </ul>"},{"location":"why-vartrack/","title":"Why varTrack?","text":""},{"location":"why-vartrack/#the-problem","title":"The problem","text":"<p>Most teams manage runtime configuration through one of two approaches \u2014 and both have serious weaknesses.</p> <p>Direct database edits</p> <ul> <li>No audit trail \u2014 who changed <code>max_connections</code> from 50 to 5 at 3 AM?</li> <li>Changes are invisible to the rest of the team</li> <li>Drift goes undetected until something breaks</li> <li>No validation \u2014 typos and bad values go straight to production</li> </ul> <p>Scattered <code>.env</code> and config files</p> <ul> <li>Duplicated across repos and deployment scripts</li> <li>Environments drift out of sync silently</li> <li>No single place to see what is deployed where</li> <li>Secrets baked into files or CI variables with no rotation story</li> </ul>"},{"location":"why-vartrack/#the-git-as-source-of-truth-model","title":"The Git-as-source-of-truth model","text":"<p>varTrack treats your Git repository as the single authority for configuration data:</p> <ul> <li>Every change is a commit \u2014 full audit trail, PR review, rollback</li> <li>Push to <code>main</code> \u2192 production synced; push to <code>develop</code> \u2192 staging synced</li> <li>Invalid configs are rejected before any write (CUE schema validation)</li> <li>Drift is caught within one poll interval and optionally healed automatically</li> </ul>"},{"location":"why-vartrack/#how-it-compares-to-argocd-and-fluxcd","title":"How it compares to ArgoCD and FluxCD","text":"<p>ArgoCD and FluxCD are the dominant GitOps tools for Kubernetes. They are excellent at what they do: reconciling Kubernetes manifests from a Git repo into a cluster. varTrack solves a different problem.</p> ArgoCD / FluxCD varTrack What it syncs Kubernetes resources (Deployments, Services, CRDs\u2026) Configuration data: MongoDB, Redis, ZooKeeper, S3, ConfigMap, Helm values, Vercel env vars, Linux files Where config lives Kubernetes etcd The application's own datastore Drift detection Kubernetes state only Any datasource \u2014 MongoDB, Redis, ZooKeeper, S3 Secret management External Secrets Operator / Sealed Secrets HashiCorp Vault with <code>@secret()</code> annotations Schema validation None CUE schema per tenant, enforced before every write Non-K8s targets \u2717 \u2713 Redis, MongoDB, ZooKeeper, S3, Linux SSH, Vercel Multi-sink fan-out \u2717 \u2713 One push \u2192 multiple sinks in parallel <p>They are complementary, not competing. A typical setup:</p> <pre><code>Git push\n  \u251c\u2500\u2500 ArgoCD \u2192 reconciles your Kubernetes Deployment\n  \u2514\u2500\u2500 varTrack \u2192 syncs runtime config into MongoDB + Redis + ConfigMap\n</code></pre>"},{"location":"why-vartrack/#how-it-compares-to-hashicorp-consul","title":"How it compares to HashiCorp Consul","text":"<p>Consul is a service mesh and distributed KV store. It doesn't help you get config into Consul from Git, validate it, or fan it out to other systems.</p> <p>varTrack can write to Consul (via ZooKeeper-compatible API or directly), with full Git history, schema validation, drift detection, and multi-env routing.</p>"},{"location":"why-vartrack/#how-it-compares-to-manual-helm-values","title":"How it compares to manual Helm values","text":"<p>Helm values files in Git are a common pattern, but they have limitations:</p> <ul> <li>Values are only applied on <code>helm upgrade</code> \u2014 no continuous reconciliation</li> <li>No fan-out to non-Helm sinks (MongoDB, Redis, etc.)</li> <li>No runtime drift detection \u2014 if someone <code>kubectl edit</code>s the ConfigMap, nobody knows</li> </ul> <p>varTrack's Helm sink triggers <code>helm upgrade</code> on push and the watcher can detect if the deployed values diverge from what Git says.</p>"},{"location":"why-vartrack/#when-vartrack-is-the-right-fit","title":"When varTrack is the right fit","text":"<p>\u2705 You store config, feature flags, or environment variables in MongoDB, Redis, S3, ZooKeeper, ConfigMaps, Helm releases, Vercel, or Linux servers \u2014 and want Git as the single authority</p> <p>\u2705 You have multiple environments (dev, staging, production) and want changes to flow automatically on push without manual scripts</p> <p>\u2705 You need drift detection \u2014 if someone edits a value directly in any sink, varTrack restores the correct state</p> <p>\u2705 You want secrets to stay in Vault and never touch Git \u2014 <code>@secret()</code> annotations resolve at sync time</p> <p>\u2705 You need to validate configs before they reach the datastore \u2014 CUE schemas catch bad values at the gate</p>"},{"location":"why-vartrack/#when-vartrack-is-not-the-right-fit","title":"When varTrack is NOT the right fit","text":"<p>\u274c You only need to sync Kubernetes manifests \u2014 ArgoCD or FluxCD is the better tool</p> <p>\u274c You want a general-purpose feature flag service (LaunchDarkly, Unleash) with gradual rollouts and targeting rules</p> <p>\u274c Your config is too dynamic to live in Git (e.g. user-generated content, real-time game state)</p>"},{"location":"cli/","title":"vt \u2014 CLI Reference","text":"<p><code>vt</code> is the command-line interface for varTrack. Push any local config file directly to a datasource from your terminal or CI/CD pipeline \u2014 without needing a Git push.</p>"},{"location":"cli/#installation","title":"Installation","text":""},{"location":"cli/#download-pre-built-binary","title":"Download pre-built binary","text":"<p>Download from the releases page for your platform, then add to your <code>PATH</code>.</p>"},{"location":"cli/#build-from-source","title":"Build from source","text":"<pre><code>cd cli\ngo build -o vt ./cmd\nmv vt /usr/local/bin/vt\n</code></pre>"},{"location":"cli/#authentication","title":"Authentication","text":""},{"location":"cli/#oidc-sso-login","title":"OIDC / SSO login","text":"<pre><code># Azure AD / Entra ID\nvt login \\\n  --server         https://gateway.example.com \\\n  --oidc-issuer    \"https://login.microsoftonline.com/&lt;tenant-id&gt;/v2.0\" \\\n  --oidc-client-id \"&lt;app-client-id&gt;\"\n\n# Google\nvt login \\\n  --server         https://gateway.example.com \\\n  --oidc-issuer    https://accounts.google.com \\\n  --oidc-client-id \"&lt;client-id&gt;\"\n</code></pre> <p>A browser window opens for login. The token is saved automatically.</p>"},{"location":"cli/#static-token-for-cicd","title":"Static token (for CI/CD)","text":"<pre><code>vt login --server https://gateway.example.com --token \"$VARTRACK_TOKEN\"\n\n# Or skip login entirely \u2014 set env vars\nexport VARTRACK_SERVER=https://gateway.example.com\nexport VARTRACK_TOKEN=eyJ...\n</code></pre>"},{"location":"cli/#commands","title":"Commands","text":""},{"location":"cli/#vt-sync","title":"<code>vt sync</code>","text":"<p>Push a local file to a datasource. Runs the full ETL pipeline.</p> <pre><code># Basic sync\nvt sync \\\n  --file       configs/app.yaml \\\n  --datasource mongo \\\n  --env        production\n\n# Dry-run \u2014 see what would be written without touching the datasource\nvt sync --file configs/app.yaml --datasource mongo --env staging --dry-run\n\n# Wait for the task to complete\nvt sync --file configs/app.yaml --datasource mongo --env production --wait\n\n# Custom timeout and label\nvt sync \\\n  --file       configs/app.yaml \\\n  --datasource mongo \\\n  --env        production \\\n  --wait       \\\n  --timeout    10m \\\n  --label      \"release/v2.3.1\"\n\n# Output raw JSON\nvt sync --file configs/app.yaml --datasource mongo --env staging --json\n</code></pre> <p>Flags:</p> Flag Default Description <code>--file</code> \u2014 Path to the config file (required) <code>--datasource</code> \u2014 Target datasource name (required) <code>--env</code> \u2014 Deployment environment (required) <code>--tenant</code> from context Tenant ID <code>--dry-run</code> false Validate without writing <code>--wait</code> false Block until task completes <code>--timeout</code> 5m Max wait time <code>--label</code> \"\" Free-form label attached to the task <code>--json</code> false Print raw JSON response"},{"location":"cli/#vt-validate","title":"<code>vt validate</code>","text":"<p>Validate a file against the CUE schema without syncing. Exits with code 1 on failure \u2014 ideal for PR checks.</p> <pre><code>vt validate --file configs/app.yaml\n\n# Specify the datasource to select the right schema\nvt validate --file configs/app.yaml --datasource mongo\n\n# With tenant\nvt validate --file configs/app.yaml --datasource mongo --tenant myapp\n</code></pre> <p>Flags:</p> Flag Default Description <code>--file</code> \u2014 Path to the config file (required) <code>--datasource</code> \u2014 Datasource name (selects the schema) <code>--tenant</code> from context Tenant ID <code>--format</code> auto File format override <code>--json</code> false Print result as JSON"},{"location":"cli/#vt-task","title":"<code>vt task</code>","text":"<p>Inspect sync tasks created by <code>vt sync</code>.</p> <pre><code>vt task get  &lt;task-id&gt;          # Get status and result\nvt task list                    # List recent tasks\nvt task list --limit 50\nvt task watch &lt;task-id&gt;         # Stream status until terminal state\n</code></pre>"},{"location":"cli/#vt-bundle-validate","title":"<code>vt bundle validate</code>","text":"<p>Check a CUE bundle file for required fields.</p> <pre><code>vt bundle validate ./config.cue\n</code></pre>"},{"location":"cli/#vt-config","title":"<code>vt config</code>","text":"<p>Manage named contexts \u2014 useful when you have multiple servers (dev, staging, production).</p> <pre><code>vt config view                          # Print config (tokens redacted)\n\nvt config set-context prod \\\n  --server https://prod-gateway.example.com \\\n  --token  \"$PROD_TOKEN\"\n\nvt config use-context prod              # Switch active context\n\nvt logout                               # Remove the active context\nvt logout --context staging\n</code></pre>"},{"location":"cli/#vt-version","title":"<code>vt version</code>","text":"<pre><code>vt version\n</code></pre>"},{"location":"cli/#environment-variables","title":"Environment variables","text":"Variable Purpose <code>VARTRACK_SERVER</code> Gateway base URL <code>VARTRACK_TOKEN</code> Bearer token \u2014 skips <code>vt login</code> <code>VARTRACK_TENANT</code> Tenant ID <code>VARTRACK_CONTEXT</code> Named context to activate"},{"location":"cli/#cicd-integration","title":"CI/CD integration","text":"<p>See the CI/CD Integration page for GitHub Actions, GitLab CI, and other pipeline examples.</p>"},{"location":"cli/cicd/","title":"CI/CD Integration","text":"<p>Use <code>vt</code> in your pipeline to validate config files on every PR and sync on every merge.</p>"},{"location":"cli/cicd/#recommended-workflow","title":"Recommended workflow","text":"<p>A typical CI/CD setup uses two steps:</p> <ol> <li>On pull request \u2014 validate the config file against the CUE schema. Block the PR if validation fails.</li> <li>On merge to main \u2014 sync the config to the production datasource.</li> </ol>"},{"location":"cli/cicd/#github-actions","title":"GitHub Actions","text":""},{"location":"cli/cicd/#validate-on-pr-sync-on-merge","title":"Validate on PR + sync on merge","text":"<pre><code>name: Config sync\n\non:\n  pull_request:\n    paths:\n      - \"configs/**\"\n  push:\n    branches: [main]\n    paths:\n      - \"configs/**\"\n\njobs:\n  validate:\n    name: Validate config\n    runs-on: ubuntu-latest\n    if: github.event_name == 'pull_request'\n    steps:\n      - uses: actions/checkout@v4\n\n      - name: Download vt\n        run: |\n          curl -sSL https://github.com/jarin-devoss/varTrack/releases/latest/download/vt_linux_amd64 \\\n            -o /usr/local/bin/vt &amp;&amp; chmod +x /usr/local/bin/vt\n\n      - name: Validate\n        env:\n          VARTRACK_SERVER: ${{ vars.VARTRACK_SERVER }}\n          VARTRACK_TOKEN:  ${{ secrets.VARTRACK_TOKEN }}\n        run: vt validate --file configs/app.yaml --datasource mongo\n\n  sync:\n    name: Sync to production\n    runs-on: ubuntu-latest\n    if: github.event_name == 'push'\n    steps:\n      - uses: actions/checkout@v4\n\n      - name: Download vt\n        run: |\n          curl -sSL https://github.com/jarin-devoss/varTrack/releases/latest/download/vt_linux_amd64 \\\n            -o /usr/local/bin/vt &amp;&amp; chmod +x /usr/local/bin/vt\n\n      - name: Sync to production\n        env:\n          VARTRACK_SERVER: ${{ vars.VARTRACK_SERVER }}\n          VARTRACK_TOKEN:  ${{ secrets.VARTRACK_TOKEN }}\n          VARTRACK_TENANT: myapp\n        run: |\n          vt sync \\\n            --file        configs/app.yaml \\\n            --datasource  mongo \\\n            --env         production \\\n            --wait \\\n            --label       \"${{ github.ref_name }}@${{ github.sha }}\"\n</code></pre>"},{"location":"cli/cicd/#multi-environment-sync","title":"Multi-environment sync","text":"<pre><code>strategy:\n  matrix:\n    include:\n      - env: staging\n        datasource: mongo-staging\n      - env: production\n        datasource: mongo-primary\n\nsteps:\n  - name: Sync ${{ matrix.env }}\n    env:\n      VARTRACK_SERVER: ${{ vars.VARTRACK_SERVER }}\n      VARTRACK_TOKEN:  ${{ secrets.VARTRACK_TOKEN }}\n    run: |\n      vt sync \\\n        --file       configs/app.yaml \\\n        --datasource ${{ matrix.datasource }} \\\n        --env        ${{ matrix.env }} \\\n        --wait\n</code></pre>"},{"location":"cli/cicd/#gitlab-ci","title":"GitLab CI","text":"<pre><code>variables:\n  VARTRACK_SERVER: https://gateway.example.com\n\nvalidate:config:\n  stage: test\n  only:\n    - merge_requests\n  script:\n    - vt validate --file configs/app.yaml --datasource mongo\n\nsync:staging:\n  stage: deploy\n  environment: staging\n  only:\n    - develop\n  script:\n    - vt sync --file configs/app.yaml --datasource mongo --env staging --wait\n\nsync:production:\n  stage: deploy\n  environment: production\n  only:\n    - main\n  script:\n    - vt sync --file configs/app.yaml --datasource mongo --env production --wait\n  when: manual\n</code></pre>"},{"location":"cli/cicd/#dry-run-on-every-pr","title":"Dry-run on every PR","text":"<p>Add a dry-run step to every PR to see exactly what would be written:</p> <pre><code>- name: Dry-run sync\n  env:\n    VARTRACK_SERVER: ${{ vars.VARTRACK_SERVER }}\n    VARTRACK_TOKEN:  ${{ secrets.VARTRACK_TOKEN }}\n  run: |\n    vt sync \\\n      --file        configs/app.yaml \\\n      --datasource  mongo \\\n      --env         production \\\n      --dry-run \\\n      --json\n</code></pre> <p>The JSON output shows the exact keys that would be written, changed, and deleted \u2014 making the diff visible in the PR.</p>"},{"location":"cli/cicd/#using-environment-variables","title":"Using environment variables","text":"<p>No <code>vt login</code> needed in CI/CD \u2014 just set the environment variables:</p> Variable Where to store <code>VARTRACK_SERVER</code> Repository variable (<code>vars.VARTRACK_SERVER</code>) <code>VARTRACK_TOKEN</code> Repository secret (<code>secrets.VARTRACK_TOKEN</code>) <code>VARTRACK_TENANT</code> Repository variable or hardcoded in the workflow"},{"location":"concepts/","title":"How varTrack Works","text":"<p>varTrack is built around a simple idea: your Git repository is the source of truth for configuration, and any datasource that holds config values should automatically stay in sync with it.</p>"},{"location":"concepts/#the-pipeline","title":"The pipeline","text":"<p>Every sync \u2014 whether triggered by a webhook or the CLI \u2014 runs through the same three-stage pipeline:</p> <pre><code>PAYLOAD       \u2192   ETL             \u2192   SYNC\n\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nresolve rule      fetch from Git      pick strategy\nmatch platform    parse file          write to sinks\n                  flatten keys        prune stale keys\n                  validate CUE        rollback on error\n</code></pre>"},{"location":"concepts/#stage-1-payload","title":"Stage 1 \u2014 Payload","text":"<p>When a webhook arrives at the gateway-service, varTrack resolves:</p> <ul> <li>Which platform sent the event (GitHub, GitLab, ...)</li> <li>Which rule matches (based on <code>datasource</code>, <code>repositories</code>, and <code>file_name</code>)</li> <li>Which environment to target (from branch name, PR number, tag, or explicit <code>branch_map</code>)</li> </ul>"},{"location":"concepts/#stage-2-etl","title":"Stage 2 \u2014 ETL","text":"<p>The orchestrator-service:</p> <ol> <li>Clones or fetches the pushed Git ref (LRU cache, max 20 repos)</li> <li>Parses the file (YAML / JSON / TOML / .env / HCL / XML / INI)</li> <li>Extracts the subtree under the <code>vartrack</code> root key (if present)</li> <li>Detects the environment key pattern (branch-keyed or flat)</li> <li>Flattens nested config to <code>{ \"key.subkey\": \"value\" }</code> using Rust BFS/DFS</li> <li>Applies <code>variables_map</code> overlay if configured</li> <li>Validates against the CUE schema for this tenant</li> </ol>"},{"location":"concepts/#stage-3-sync","title":"Stage 3 \u2014 Sync","text":"<p>Writes the validated flat map to every configured sink:</p> <ul> <li>Picks the sync strategy (<code>AUTO</code>, <code>GIT_UPSERT_ALL</code>, <code>GIT_SMART_REPAIR</code>, or <code>LIVE_STATE</code>)</li> <li>Runs writes to all matching sinks in parallel</li> <li>Prunes stale keys if <code>prune: true</code> is set on the rule</li> <li>Rolls back (deletes written data) if a write fails partway through</li> </ul>"},{"location":"concepts/#services","title":"Services","text":"Service Language Port Role gateway-service Go 5657 HTTP webhook receiver, platform routing, gRPC forwarder orchestrator-service Python + Rust 8000 / 50051 ETL pipeline, CUE validation, multi-sink writer watcher-service Go 9091 Drift detection, self-heal trigger vt Go \u2014 CLI for manual sync and CI/CD"},{"location":"concepts/#data-flow-diagram","title":"Data flow diagram","text":"<pre><code>                    \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n  GitHub push  \u2500\u2500\u2500\u25ba \u2502          gateway-service             \u2502\n                    \u2502  \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510  \u2502\n                    \u2502  \u2502  verify signature               \u2502  \u2502\n                    \u2502  \u2502  match rule                     \u2502  \u2502\n                    \u2502  \u2502  rate limit                     \u2502  \u2502\n                    \u2502  \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518  \u2502\n                    \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n                                     \u2502 gRPC\n                    \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u25bc\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n                    \u2502        orchestrator-service           \u2502\n                    \u2502  \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510  \u2502\n                    \u2502  \u2502  fetch Git ref (LRU cache)      \u2502  \u2502\n                    \u2502  \u2502  parse + flatten (Rust)         \u2502  \u2502\n                    \u2502  \u2502  validate CUE schema            \u2502  \u2502\n                    \u2502  \u2502  write to sinks                 \u2502\u2500\u2500\u253c\u2500\u2500\u25ba MongoDB\n                    \u2502  \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518  \u2502\u2500\u2500\u25ba Redis\n                    \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\u2500\u2500\u25ba ZooKeeper\n                                                               \u2500\u2500\u25ba S3 / ...\n                    \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n                    \u2502          watcher-service              \u2502\n                    \u2502  polls datasources every 60 s        \u2502\n                    \u2502  compares live state vs Git baseline  \u2502\n                    \u2502  drift detected \u2192 TriggerSync (gRPC) \u2502\n                    \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n</code></pre>"},{"location":"concepts/#rust-core","title":"Rust core","text":"<p>Hot-path operations (flattening, merging, diffing, hashing) are implemented in Rust and exposed to Python via PyO3. Pure-Python fallbacks activate automatically if the Rust extension is not compiled \u2014 so the service always works, just slightly slower without it.</p> Module Purpose <code>flatten</code> BFS / DFS nested dict \u2192 flat <code>key.subkey</code> map <code>merge</code> Variable map overlay + environment resolution <code>prune</code> Stale-key detection and deferred deletion <code>sync</code> BLAKE3 content hash for <code>GIT_SMART_REPAIR</code>"},{"location":"concepts/#next","title":"Next","text":"<ul> <li>Destination template \u2014 control where data lands per rule</li> <li>Environment resolution \u2014 map branches, PRs, and tags to environments</li> <li>Sync strategies \u2014 choose how writes are applied</li> <li>Drift detection \u2014 how the watcher catches and heals config drift</li> </ul>"},{"location":"concepts/destination-template/","title":"Destination Template","text":"<p><code>destination_template</code> is a rule-level field that controls exactly where data lands in each sink \u2014 the collection name, key prefix, znode path, file path, or release name \u2014 using placeholders resolved at sync time.</p>"},{"location":"concepts/destination-template/#placeholders","title":"Placeholders","text":"Placeholder Resolved from <code>{env}</code> Resolved environment (e.g. <code>production</code>, <code>staging</code>, <code>pr-42</code>) <code>{tenant}</code> Tenant ID from the <code>X-Tenant-ID</code> request header"},{"location":"concepts/destination-template/#per-sink-behavior","title":"Per-sink behavior","text":"Sink What it controls Template Resolved value MongoDB Collection name <code>\"{env}-config\"</code> <code>production-config</code> ZooKeeper znode root path <code>\"/{tenant}/{env}\"</code> <code>/acme/pr-42</code> Redis Key prefix <code>\"{env}:cfg\"</code> <code>production:cfg</code> S3 Key prefix <code>\"{tenant}/{env}/\"</code> <code>acme/production/</code> Linux server Full remote file path <code>\"/etc/app/{env}.env\"</code> <code>/etc/app/production.env</code> Helm Release name <code>\"app-{env}\"</code> <code>app-production</code> ConfigMap ConfigMap name <code>\"myapp-{env}\"</code> <code>myapp-production</code> Vercel Environment variable prefix <code>\"{env}_\"</code> <code>production_</code>"},{"location":"concepts/destination-template/#examples","title":"Examples","text":""},{"location":"concepts/destination-template/#one-template-three-environments","title":"One template, three environments","text":"<pre><code>rules: [{\n  platform:             \"github\"\n  datasource:           \"mongo\"\n  file_name:            \"configs/app.yaml\"\n  repositories:         [\"my-org/*\"]\n  destination_template: \"{env}-config\"\n  branch_map: {\n    main:    \"production\"\n    develop: \"staging\"\n    test:    \"test\"\n  }\n}]\n</code></pre> Push branch Resolved env Collection written to <code>main</code> <code>production</code> <code>production-config</code> <code>develop</code> <code>staging</code> <code>staging-config</code> <code>test</code> <code>test</code> <code>test-config</code>"},{"location":"concepts/destination-template/#per-tenant-isolation","title":"Per-tenant isolation","text":"<pre><code>destination_template: \"{tenant}/{env}-config\"\n</code></pre> <p>A push from tenant <code>acme</code> to <code>main</code> writes to <code>acme/production-config</code>. A push from tenant <code>globex</code> to <code>main</code> writes to <code>globex/production-config</code>.</p>"},{"location":"concepts/destination-template/#pr-preview-environments","title":"PR preview environments","text":"<pre><code>rules: [{\n  platform:             \"github\"\n  datasource:           \"mongo\"\n  file_name:            \"configs/app.yaml\"\n  repositories:         [\"my-org/app\"]\n  destination_template: \"{env}-config\"\n  env_as_pr:            true    // PR #42 \u2192 env = \"pr-42\"\n}]\n</code></pre> <p>Each pull request gets its own isolated collection: <code>pr-42-config</code>, <code>pr-103-config</code>, etc.</p>"},{"location":"concepts/destination-template/#zookeeper-path-per-tenant-and-environment","title":"ZooKeeper path per tenant and environment","text":"<pre><code>rules: [{\n  platform:             \"github\"\n  datasource:           \"zookeeper\"\n  destination_template: \"/{tenant}/{env}\"\n}]\n</code></pre> <p>Writes znodes under <code>/acme/production/</code>, <code>/acme/staging/</code>, etc.</p>"},{"location":"concepts/destination-template/#validation","title":"Validation","text":"<p>If <code>{env}</code> appears in the template, at least one environment resolution strategy must be enabled:</p> <ul> <li><code>env_as_branch: true</code></li> <li><code>env_as_pr: true</code></li> <li><code>env_as_tags: true</code></li> <li><code>branch_map: { ... }</code></li> <li><code>file_path_map: { ... }</code></li> </ul> <p>varTrack will reject the bundle at startup if <code>{env}</code> is used with no resolution strategy configured.</p>"},{"location":"concepts/destination-template/#fallback-behavior","title":"Fallback behavior","text":"<p>If <code>destination_template</code> is not set, each sink falls back to its own sink-specific configuration key (e.g. <code>collection</code> for MongoDB, <code>key_prefix</code> for Redis).</p>"},{"location":"concepts/drift-detection/","title":"Drift Detection &amp; Self-Heal","text":"<p>The watcher-service continuously polls every configured datasource and compares the live state against the Git baseline. When values diverge, it can restore the correct state automatically.</p>"},{"location":"concepts/drift-detection/#what-is-drift","title":"What is drift?","text":"<p>Drift happens when a datasource value no longer matches what Git says it should be. Common causes:</p> <ul> <li>A developer edits MongoDB directly during an incident</li> <li>A deployment script overwrites a config value</li> <li>A Kubernetes operator changes a ConfigMap</li> <li>A Redis key expires or is flushed</li> </ul> <p>Without drift detection, these changes go unnoticed until something breaks.</p>"},{"location":"concepts/drift-detection/#how-detection-works","title":"How detection works","text":"<pre><code>Every POLL_INTERVAL (default: 60s)\n  \u2502\n  \u251c\u2500\u2500 Read live values from datasource\n  \u251c\u2500\u2500 Compare against Git-sourced baseline (key by key)\n  \u2502\n  \u251c\u2500\u2500 No diff   \u2192 log \"OK\", update poll counter\n  \u2502\n  \u2514\u2500\u2500 Diff found\n        \u251c\u2500\u2500 Increment vartrack_watcher_drift_total counter\n        \u251c\u2500\u2500 Log the drifted keys and their values\n        \u2514\u2500\u2500 self_heal: true?\n              \u251c\u2500\u2500 Yes \u2192 call TriggerSync gRPC \u2192 orchestrator re-runs ETL pipeline\n              \u2514\u2500\u2500 No  \u2192 log drift, alert via metrics, do nothing\n</code></pre>"},{"location":"concepts/drift-detection/#enabling-self-heal","title":"Enabling self-heal","text":"<p>Set <code>self_heal: true</code> on a rule to enable automatic repair:</p> <pre><code>rules: [{\n  platform:   \"github\"\n  datasource: \"mongo\"\n  self_heal:  true    // watcher will restore state on drift\n}]\n</code></pre> <p>Rules with <code>self_heal: false</code> (or omitted) are still polled and drift is logged, but no automatic repair is triggered.</p>"},{"location":"concepts/drift-detection/#concrete-example","title":"Concrete example","text":"<p>Your config in Git:</p> <pre><code>max_connections: 50\nlog_level: info\n</code></pre> <p>Someone runs <code>db.variables.updateOne({key: \"max_connections\"}, {$set: {value: 5}})</code> directly in MongoDB.</p> <p>On the next poll cycle:</p> <ol> <li>Watcher reads <code>max_connections = 5</code> from MongoDB</li> <li>Compares against baseline: expected <code>50</code>, got <code>5</code></li> <li><code>self_heal: true</code> \u2192 calls <code>TriggerSync</code> on orchestrator</li> <li>Orchestrator fetches the file from Git, parses it, writes <code>max_connections = 50</code> back to MongoDB</li> <li>Next poll sees no drift \u2014 state restored</li> </ol> <p>Total recovery time: up to one <code>POLL_INTERVAL</code> (default 60 seconds).</p>"},{"location":"concepts/drift-detection/#metrics","title":"Metrics","text":"<p>Monitor drift in Prometheus / Grafana:</p> Metric Type Description <code>vartrack_watcher_poll_total</code> Counter Total poll cycles, labeled by datasource <code>vartrack_watcher_drift_total</code> Counter Drift events detected <code>vartrack_watcher_heal_total</code> Counter Self-heal calls triggered <code>vartrack_watcher_heal_errors_total</code> Counter Self-heal calls that failed <code>vartrack_watcher_poll_duration_seconds</code> Histogram Poll cycle duration"},{"location":"concepts/drift-detection/#poll-interval","title":"Poll interval","text":"<p>Adjust how often the watcher checks each datasource:</p> <pre><code>POLL_INTERVAL=30s go run ./watcher-service/cmd\n</code></pre> <p>Lower values mean faster drift detection but more load on datasources.</p>"},{"location":"concepts/drift-detection/#why-polling-and-how-its-kept-efficient","title":"Why polling? (and how it's kept efficient)","text":"<p>varTrack uses a scheduled pull model rather than event-driven push because it must support eight fundamentally different backends \u2014 MongoDB, Redis, ZooKeeper, S3, Kubernetes ConfigMaps, Helm, Vercel, and Linux servers. Each backend exposes a different (or no) change-notification mechanism:</p> Datasource Native change events? MongoDB Change Streams (replica set only) Redis Keyspace notifications (off by default, extra config) ZooKeeper Watches (per-znode, limited concurrency) S3 Event notifications (requires SNS/SQS wiring) Linux server None \u2014 SSH file stat only Vercel None ConfigMap Kubernetes informer (requires in-cluster) Helm None \u2014 release state only <p>A unified polling model means a single code path works across all backends with no external infrastructure requirements.</p> <p>How the overhead is minimised:</p> <ul> <li>Lightweight reads \u2014 for key/value stores (Redis, MongoDB document), the watcher fetches only the keys declared in the rule's config file, not the entire database.</li> <li>Configurable interval \u2014 default 60 s; set <code>POLL_INTERVAL</code> to match your SLA.</li> <li>Leader election \u2014 in multi-replica deployments, only one replica polls at a time (ZooKeeper or Redis distributed lock).</li> <li>Shared state store \u2014 replicas share the baseline via Redis so each poll compares against a consistent snapshot.</li> <li>Circuit breaker \u2014 if a datasource is unreachable, the watcher backs off exponentially rather than hammering it.</li> </ul>"},{"location":"concepts/drift-detection/#multi-replica-deployments","title":"Multi-replica deployments","text":"<p>When running multiple watcher replicas, only one should run the heal loop at a time. Leader election ensures this:</p> <pre><code>global_tags: {\n  watcher_leader_election_datasource: \"redis\"   // or \"zookeeper\"\n}\n</code></pre> <p>Replicas that are not the leader still poll and detect drift, but defer healing to the current leader. If the leader crashes, another replica acquires the lock within one TTL window (15 seconds for Redis).</p>"},{"location":"concepts/drift-detection/#shared-state-store","title":"Shared state store","text":"<p>In multi-replica deployments, all replicas can share the same baseline state via Redis:</p> <pre><code>global_tags: {\n  watcher_state_redis: \"redis\"   // name of a configured redis datasource\n}\n</code></pre> <p>This prevents replicas from triggering unnecessary heals due to stale local state.</p>"},{"location":"concepts/environment-resolution/","title":"Environment Resolution","text":"<p>When a webhook fires, varTrack resolves a single environment string from the event. This string is used to:</p> <ul> <li>Resolve <code>{env}</code> in <code>destination_template</code></li> <li>Select the correct subtree from env-keyed config files</li> <li>Determine which sink collection, prefix, or path to write to</li> </ul>"},{"location":"concepts/environment-resolution/#resolution-order","title":"Resolution order","text":"<p>varTrack tries each strategy in order and uses the first one that produces a match:</p> Priority Strategy Example 1 <code>branch_map[branch]</code> <code>main</code> \u2192 <code>production</code> 2 <code>file_path_map[file_path]</code> <code>configs/prod/**</code> \u2192 <code>production</code> 3 <code>env_as_pr: true</code> PR #42 \u2192 <code>pr-42</code> 4 <code>env_as_branch: true</code> branch <code>develop</code> \u2192 <code>develop</code> 5 <code>env_as_tags: true</code> tag <code>v1.2.3</code> \u2192 <code>v1.2.3</code> 6 fallback always \u2192 <code>default</code>"},{"location":"concepts/environment-resolution/#branch_map-explicit-mapping","title":"<code>branch_map</code> \u2014 explicit mapping","text":"<p>Map specific branch names to environment names:</p> <pre><code>rules: [{\n  platform:   \"github\"\n  datasource: \"mongo\"\n  branch_map: {\n    main:    \"production\"\n    develop: \"staging\"\n    qa:      \"qa\"\n  }\n}]\n</code></pre> <p>Pushes to <code>main</code> write to <code>production-config</code>. Pushes to <code>develop</code> write to <code>staging-config</code>. Any branch not in the map falls through to the next strategy.</p>"},{"location":"concepts/environment-resolution/#file_path_map-path-glob-mapping","title":"<code>file_path_map</code> \u2014 path glob mapping","text":"<p>Map file paths (glob patterns) to environment names:</p> <pre><code>rules: [{\n  platform:   \"github\"\n  datasource: \"mongo\"\n  file_path_map: {\n    \"configs/production/**\": \"production\"\n    \"configs/staging/**\":    \"staging\"\n  }\n}]\n</code></pre> <p>A push that changes <code>configs/production/app.yaml</code> resolves to <code>production</code>.</p>"},{"location":"concepts/environment-resolution/#env_as_pr-pr-environments","title":"<code>env_as_pr</code> \u2014 PR environments","text":"<pre><code>env_as_pr: true\n</code></pre> <p>Pull request events resolve to <code>pr-{number}</code>. Useful for ephemeral preview environments.</p> <pre><code>PR #42  \u2192 env = \"pr-42\"\nPR #103 \u2192 env = \"pr-103\"\n</code></pre>"},{"location":"concepts/environment-resolution/#env_as_branch-branch-name-as-environment","title":"<code>env_as_branch</code> \u2014 branch name as environment","text":"<pre><code>env_as_branch: true\n</code></pre> <p>The branch name becomes the environment name directly:</p> <pre><code>branch \"develop\"     \u2192 env = \"develop\"\nbranch \"feature/foo\" \u2192 env = \"feature/foo\"\n</code></pre>"},{"location":"concepts/environment-resolution/#env_as_tags-tag-name-as-environment","title":"<code>env_as_tags</code> \u2014 tag name as environment","text":"<pre><code>env_as_tags: true\n</code></pre> <p>Tag push events use the tag name as the environment:</p> <pre><code>tag \"v1.2.3\"    \u2192 env = \"v1.2.3\"\ntag \"release-5\" \u2192 env = \"release-5\"\n</code></pre>"},{"location":"concepts/environment-resolution/#root_key-extracting-a-subtree-before-slicing","title":"<code>root_key</code> \u2014 extracting a subtree before slicing","text":"<p>If your config file has a wrapper key, set <code>root_key</code> in the rule to unwrap it first. varTrack extracts the subtree under that key, then runs env auto-detection on it.</p> <pre><code>rules: [{\n  root_key: \"vartrack\"   // default \u2014 extract the \"vartrack\" subtree if it exists\n}]\n</code></pre> <pre><code># configs/app.yaml\nvartrack:\n  production:\n    database_host: mongo.prod.internal\n  staging:\n    database_host: mongo.staging.internal\nother_tool:\n  setting: ignored      # not under root_key \u2014 never touched\n</code></pre> <p><code>root_key: \"vartrack\"</code> \u2192 extracts the inner dict \u2192 Pattern 1 runs on it \u2192 <code>database_host=mongo.prod.internal</code> for <code>production</code>.</p> <p>Set <code>root_key: \"\"</code> to always process the whole file regardless of structure.</p> <p>If the key doesn't exist in the file, varTrack soft-falls back to the whole file.</p>"},{"location":"concepts/environment-resolution/#auto-detection-from-config-file-structure","title":"Auto-detection from config file structure","text":"<p>When your config file itself is keyed by environment, varTrack auto-detects the structure and extracts only the relevant slice \u2014 no extra rule config needed. Three patterns are supported.</p>"},{"location":"concepts/environment-resolution/#pattern-1-all-top-level-keys-are-env-names","title":"Pattern 1 \u2014 all top-level keys are env names","text":"<p>Every top-level value is a dict. varTrack merges <code>default</code> first, then the matching env overrides it.</p> <pre><code># configs/app.yaml\nproduction:\n  database_host: mongo.prod.internal\n  max_connections: 100\nstaging:\n  database_host: mongo.staging.internal\n  max_connections: 10\ndefault:\n  max_connections: 5    # fallback for unknown envs\n  log_level: info\n</code></pre> env result <code>production</code> <code>database_host=mongo.prod.internal</code>, <code>max_connections=100</code>, <code>log_level=info</code> <code>staging</code> <code>database_host=mongo.staging.internal</code>, <code>max_connections=10</code>, <code>log_level=info</code> <code>pr-42</code> <code>max_connections=5</code>, <code>log_level=info</code> (from <code>default</code>)"},{"location":"concepts/environment-resolution/#pattern-2-per-field-env-sub-keys-mixed-with-scalars","title":"Pattern 2 \u2014 per-field env sub-keys mixed with scalars","text":"<p>Each field that varies per env has a dict of env\u2192value. Fields that are the same across all envs are plain scalars.</p> <pre><code># configs/app.yaml\ndatabase_host:\n  production: mongo.prod.internal\n  staging:    mongo.staging.internal\n  default:    mongo.local\nmax_connections: 50     # same for every env \u2014 written as-is\nlog_level: info\n</code></pre> env result <code>production</code> <code>database_host=mongo.prod.internal</code>, <code>max_connections=50</code>, <code>log_level=info</code> <code>staging</code> <code>database_host=mongo.staging.internal</code>, <code>max_connections=50</code>, <code>log_level=info</code> <code>pr-42</code> <code>database_host=mongo.local</code>, <code>max_connections=50</code>, <code>log_level=info</code>"},{"location":"concepts/environment-resolution/#pattern-3-env-dicts-and-scalar-defaults-at-the-same-level","title":"Pattern 3 \u2014 env dicts and scalar defaults at the same level","text":"<p>Some keys at the same nesting level are env names (dicts with overrides), others are plain scalars that apply to every env. Order in the file doesn't matter \u2014 varTrack categorizes by value type, not position. The env dict is merged on top of the scalars.</p> <pre><code>{\n    \"production\": { \"name\": \"worn\",  \"db\": \"mongo.prod.internal\" },\n    \"predev\":     { \"name\": \"dan\",   \"db\": \"mongo.dev.internal\"  },\n    \"name\":       \"bob\",\n    \"age\":        33,\n    \"timeout\":    30\n}\n</code></pre> env result <code>production</code> <code>name=worn</code>, <code>db=mongo.prod.internal</code>, <code>age=33</code>, <code>timeout=30</code> <code>predev</code> <code>name=dan</code>, <code>db=mongo.dev.internal</code>, <code>age=33</code>, <code>timeout=30</code> <code>unknown</code> (no match, no <code>default</code> dict) data returned unchanged <p>Add a <code>default</code> dict to handle unknown envs:</p> <pre><code>{\n    \"production\": { \"name\": \"worn\" },\n    \"default\":    { \"name\": \"bob\" },\n    \"age\":        33\n}\n</code></pre> <p><code>unknown</code> env \u2192 <code>name=bob</code>, <code>age=33</code>.</p>"},{"location":"concepts/environment-resolution/#pattern-3-env-named-top-level-dicts-mixed-with-scalar-defaults","title":"Pattern 3 \u2014 env-named top-level dicts mixed with scalar defaults","text":"<pre><code>{\n    \"prod\":   { \"name\": \"worn\" },\n    \"predev\": { \"name\": \"dan\" },\n    \"name\":   \"bob\",\n    \"age\":    33\n}\n</code></pre> <p>Top-level dicts whose key matches the resolved env are applied as overrides. Top-level scalars are universal defaults merged underneath:</p> env result <code>prod</code> <code>name=worn</code>, <code>age=33</code> <code>predev</code> <code>name=dan</code>, <code>age=33</code> <code>unknown</code> (no matching dict, no <code>default</code> dict) data returned unchanged <p>You can also include a <code>default</code> dict for unknown env fallback:</p> <pre><code>{\n    \"prod\":    { \"name\": \"worn\" },\n    \"default\": { \"name\": \"bob\" },\n    \"age\":     33\n}\n</code></pre> <p><code>unknown</code> env \u2192 <code>name=bob</code>, <code>age=33</code>.</p>"},{"location":"concepts/environment-resolution/#default-fallback","title":"<code>default</code> fallback","text":"<p>In all three patterns, if the resolved env has no matching key, varTrack silently falls back to <code>default</code>. If there is no <code>default</code> either, the data is returned unchanged.</p>"},{"location":"concepts/environment-resolution/#root_key-extracting-a-subtree-before-slicing_1","title":"<code>root_key</code> \u2014 extracting a subtree before slicing","text":"<p>Real config files often live alongside other tooling config (Spring, Maven, npm, etc.). Use <code>root_key</code> to extract only the varTrack-relevant subtree before env-detection runs.</p> <pre><code>rules: [{\n  root_key: \"vartrack\"   // default \u2014 extract \"vartrack\" subtree if present\n}]\n</code></pre> <p>Set <code>root_key: \"\"</code> to always process the whole file. If the key doesn't exist, varTrack soft-falls back to the whole file.</p>"},{"location":"concepts/environment-resolution/#example-packagejson","title":"Example \u2014 <code>package.json</code>","text":"<p>A Node.js project that also carries deploy config inside <code>package.json</code>:</p> <pre><code>{\n  \"name\": \"my-app\",\n  \"version\": \"1.0.0\",\n  \"scripts\": { \"start\": \"node index.js\" },\n  \"vartrack\": {\n    \"production\": { \"API_URL\": \"https://api.prod.com\",  \"LOG_LEVEL\": \"warn\"  },\n    \"staging\":    { \"API_URL\": \"https://api.stg.com\",   \"LOG_LEVEL\": \"info\"  },\n    \"default\":    { \"API_URL\": \"https://api.local.com\", \"LOG_LEVEL\": \"debug\" }\n  }\n}\n</code></pre> <pre><code>rules: [{\n  file_name: \"package.json\"\n  root_key:  \"vartrack\"       // ignore name/version/scripts \u2014 only sync vartrack block\n  branch_map: { main: \"production\", develop: \"staging\" }\n}]\n</code></pre> <p>Push to <code>main</code> \u2192 <code>API_URL=https://api.prod.com</code>, <code>LOG_LEVEL=warn</code> written to the sink.</p>"},{"location":"concepts/environment-resolution/#example-pomxml-maven","title":"Example \u2014 <code>pom.xml</code> (Maven)","text":"<p>A Java project with deploy properties in <code>pom.xml</code>. varTrack parses XML and extracts the subtree:</p> <pre><code>&lt;project&gt;\n  &lt;groupId&gt;com.example&lt;/groupId&gt;\n  &lt;artifactId&gt;my-service&lt;/artifactId&gt;\n\n  &lt;vartrack&gt;\n    &lt;production&gt;\n      &lt;database_host&gt;mongo.prod.internal&lt;/database_host&gt;\n      &lt;max_connections&gt;100&lt;/max_connections&gt;\n    &lt;/production&gt;\n    &lt;staging&gt;\n      &lt;database_host&gt;mongo.staging.internal&lt;/database_host&gt;\n      &lt;max_connections&gt;10&lt;/max_connections&gt;\n    &lt;/staging&gt;\n    &lt;default&gt;\n      &lt;max_connections&gt;5&lt;/max_connections&gt;\n    &lt;/default&gt;\n  &lt;/vartrack&gt;\n&lt;/project&gt;\n</code></pre> <pre><code>rules: [{\n  file_name: \"pom.xml\"\n  root_key:  \"vartrack\"\n  branch_map: { main: \"production\", develop: \"staging\" }\n}]\n</code></pre> <p>Push to <code>main</code> \u2192 <code>database_host=mongo.prod.internal</code>, <code>max_connections=100</code>.</p>"},{"location":"concepts/environment-resolution/#combining-strategies","title":"Combining strategies","text":"<p>Strategies can be combined. varTrack uses the first one that matches:</p> <pre><code>rules: [{\n  platform:   \"github\"\n  datasource: \"mongo\"\n  branch_map: {\n    main:    \"production\"   // explicit \u2014 highest priority\n    develop: \"staging\"\n  }\n  env_as_pr:     true       // fallback for PRs not in branch_map\n  env_as_branch: true       // fallback for all other branches\n}]\n</code></pre> Event Resolved env Push to <code>main</code> <code>production</code> (from <code>branch_map</code>) Push to <code>develop</code> <code>staging</code> (from <code>branch_map</code>) PR #42 opened <code>pr-42</code> (from <code>env_as_pr</code>) Push to <code>feature/x</code> <code>feature/x</code> (from <code>env_as_branch</code>)"},{"location":"concepts/sync-strategies/","title":"Sync Strategies","text":"<p>A sync strategy controls how varTrack applies the flattened config values to a datasource. The right strategy depends on the size of your config and how often you need reads vs. writes.</p>"},{"location":"concepts/sync-strategies/#strategies","title":"Strategies","text":"AUTO (recommended)GIT_UPSERT_ALLGIT_SMART_REPAIRLIVE_STATE <p>varTrack picks the strategy automatically based on payload size:</p> <ul> <li>\u2264 500 keys \u2192 <code>GIT_UPSERT_ALL</code></li> <li> <p>500 keys \u2192 <code>GIT_SMART_REPAIR</code></p> </li> </ul> <p>This is the default and works well for most configurations. No tuning needed.</p> <pre><code>sync_mode: \"AUTO\"\n</code></pre> <p>Writes every key from the flattened Git payload to the datasource in one bulk operation. Does not read the current state first.</p> <p>Best for: Small configs (&lt; 500 keys), initial bootstrapping, or when you always want a clean overwrite.</p> <pre><code>sync_mode: \"SYNC_MODE_GIT_UPSERT_ALL\"\n</code></pre> Read before write No Keys written All Stale key cleanup Only if <code>prune: true</code> set Best for Small configs <p>Reads the current live state first, computes a diff (added / changed / removed keys), and writes only the differences. Uses BLAKE3 content hashing to detect unchanged values.</p> <p>Best for: Large configs (&gt; 500 keys), or datasources where minimizing write operations matters.</p> <pre><code>sync_mode: \"SYNC_MODE_GIT_SMART_REPAIR\"\n</code></pre> Read before write Yes Keys written Changed only Stale key cleanup Yes, on diff Best for Large configs <p>Replaces the entire stored state with Git's current view. No diff computed \u2014 direct overwrite.</p> <p>Best for: Low-latency sinks, or scenarios where the simplest possible write path matters more than efficiency.</p> <pre><code>sync_mode: \"SYNC_MODE_LIVE_STATE\"\n</code></pre>"},{"location":"concepts/sync-strategies/#setting-the-strategy","title":"Setting the strategy","text":"<p>Override per rule in <code>config.cue</code>:</p> <pre><code>rules: [{\n  platform:   \"github\"\n  datasource: \"mongo\"\n  sync_mode:  \"SYNC_MODE_GIT_SMART_REPAIR\"\n}]\n</code></pre> <p>Or let <code>AUTO</code> decide \u2014 just omit <code>sync_mode</code> or set it to <code>\"AUTO\"</code>.</p>"},{"location":"concepts/sync-strategies/#stale-key-pruning","title":"Stale key pruning","text":"<p>When keys are removed from your Git config file, varTrack can automatically delete them from the datasource:</p> <pre><code>rules: [{\n  platform:   \"github\"\n  datasource: \"mongo\"\n  sync_mode:  \"AUTO\"\n  prune:      true     // delete keys present in datasource but missing from Git\n}]\n</code></pre> <p>Warning</p> <p><code>prune: true</code> permanently deletes keys from the datasource that are no longer in Git. Use <code>prune_dry_run</code> or the full dry-run first to verify what would be removed.</p>"},{"location":"concepts/sync-strategies/#prune_last","title":"<code>prune_last</code>","text":"<p>When multiple files map to the same datasource, deletions are deferred until all files have been written. This prevents a key from being pruned because it was removed from one file but still exists in another:</p> <pre><code>rules: [{\n  prune:      true\n  prune_last: true   // safe for multi-file rules\n}]\n</code></pre>"},{"location":"concepts/sync-strategies/#prune_protection","title":"<code>prune_protection</code>","text":"<p>Glob patterns for keys that must never be deleted, even if they're absent from Git:</p> <pre><code>rules: [{\n  prune:            true\n  prune_protection: [\"SYSTEM_*\", \"_vt_*\", \"readonly.*\"]\n}]\n</code></pre>"},{"location":"concepts/sync-strategies/#dry-run","title":"Dry-run","text":""},{"location":"concepts/sync-strategies/#full-dry-run-simulate-the-entire-sync","title":"Full dry-run \u2014 simulate the entire sync","text":"<p>No writes, no deletes. Returns a full report of what would be written, changed, and deleted \u2014 including which keys would be pruned.</p> <pre><code># Via CLI\nvt sync --file configs/app.yaml --datasource mongo --env staging --dry-run\n\n# Via HTTP\nPOST /v1/webhooks/mongo/dry-run\n</code></pre> <p><code>@secret</code>-annotated fields are fetched from Vault but masked as <code>***</code> in the report.</p>"},{"location":"concepts/sync-strategies/#prune-dry-run-simulate-deletes-only","title":"Prune dry-run \u2014 simulate deletes only","text":"<p>Test the prune step independently without touching any writes. Enabled per rule:</p> <pre><code>rules: [{\n  prune:         true\n  dry_run_prune: true   // always simulate deletes \u2014 never actually remove keys\n}]\n</code></pre> <p>With <code>dry_run_prune: true</code>, every sync runs normally (writes go through) but the delete step is always simulated and logged, never executed. Useful for auditing which stale keys would be removed before committing to live pruning.</p>"},{"location":"configuration/annotations/","title":"CUE Schema Annotations","text":"<p>varTrack supports two custom annotations in CUE schema files: <code>@secret()</code> and <code>@logger()</code>. Both are declared on fields in your schema, not in your config files.</p>"},{"location":"configuration/annotations/#secret","title":"<code>@secret()</code>","text":"<p>Marks a field whose value is a Vault path at ETL time instead of a real value. Before writing to any sink, the orchestrator reads the schema, finds every <code>@secret</code>-annotated field, fetches the real value from Vault, and injects it in-place.</p>"},{"location":"configuration/annotations/#syntax","title":"Syntax","text":"<pre><code>// default secret manager\n\"db.password\": string @secret()\n\n// named secret manager (matches secret_managers[].vault tag in the bundle)\n\"api_key\": string @secret(ref=\"other-vault\")\n</code></pre>"},{"location":"configuration/annotations/#config-file-side","title":"Config file side","text":"<p>In the actual config file being synced (YAML, JSON, TOML, etc.) the field value is the Vault path:</p> <pre><code># configs/app.yaml\ndatabase:\n  password: \"@secret(secret/myapp/db#password)\"\n\nauth:\n  jwt_secret: \"@secret(secret/myapp/auth#jwt_secret)\"\n</code></pre> <p>The format is <code>@secret(mount/path#field)</code> \u2014 mount path + <code>#</code> + field name inside the Vault secret.</p>"},{"location":"configuration/annotations/#what-happens-at-etl-time","title":"What happens at ETL time","text":"<pre><code>1. Parse schema \u2192 find @secret-annotated fields\n2. For each field present in flat_data:\n   a. Determine secret manager: ref= or \"default\"\n   b. Fetch value from Vault: mount/path \u2192 field\n   c. Replace flat_data[field] with the real value\n3. Write to sink \u2014 Vault value arrives, not the path string\n</code></pre>"},{"location":"configuration/annotations/#dry-run-behaviour","title":"Dry-run behaviour","text":"<p>In dry-run mode, <code>@secret</code> fields are masked as <code>***</code> in the report. The real value is fetched from Vault but never returned to the caller.</p>"},{"location":"configuration/annotations/#bundle-wiring","title":"Bundle wiring","text":"<p>The <code>ref=</code> value must match the <code>tag</code> of a secret manager declared in the bundle:</p> <pre><code>bundle: {\n  secret_managers: [\n    {\n      vault: {\n        tag:         \"default\"   // matched by @secret()\n        endpoint:    \"https://vault.mycompany.com\"\n        mount_point: \"secret\"\n        kv_version:  2\n        token_auth: { token: \"hvs.xxxx\" }\n      }\n    },\n    {\n      vault: {\n        tag:         \"other-vault\"   // matched by @secret(ref=\"other-vault\")\n        endpoint:    \"https://vault2.mycompany.com\"\n        mount_point: \"secret\"\n        kv_version:  2\n        token_auth: { token: \"hvs.yyyy\" }\n      }\n    },\n  ]\n}\n</code></pre>"},{"location":"configuration/annotations/#logger","title":"<code>@logger()</code>","text":"<p>Marks a field for structured change logging. Every time a value changes between ETL runs, the orchestrator emits an INFO log entry showing the field name, old value, new value, datasource, and environment.</p>"},{"location":"configuration/annotations/#syntax_1","title":"Syntax","text":"<pre><code>app_port:      int    @logger()\ndatabase.host: string @logger()\n</code></pre>"},{"location":"configuration/annotations/#log-output","title":"Log output","text":"<p>When a non-secret field changes:</p> <pre><code>field changed  datasource=mongo  env=production  field=app_port\n               from=8080  to=9090  file=configs/app.yaml\n</code></pre> <p>When a field is written for the first time (no old value):</p> <pre><code>field changed  datasource=mongo  env=production  field=database.host\n               to=\"db.prod.internal\"  file=configs/app.yaml\n</code></pre>"},{"location":"configuration/annotations/#combining-with-secret","title":"Combining with <code>@secret()</code>","text":"<p>You can annotate a field with both. The change is logged but the values are never revealed \u2014 only <code>changed=true</code> is recorded:</p> <pre><code>\"database.password\": string @secret() @logger()\n</code></pre> <pre><code>field changed  datasource=mongo  env=production  field=database.password\n               changed=true  file=configs/app.yaml\n</code></pre>"},{"location":"configuration/annotations/#full-schema-example","title":"Full schema example","text":"<pre><code>// schemas/myapp.cue\n#Config: {\n  app_port:          int &amp; &gt;=1024 &amp; &lt;=65535   @logger()\n  log_level:         \"debug\" | \"info\" | \"warn\" | \"error\"\n  \"database.host\":   string &amp; !=\"\"            @logger()\n  \"database.port\":   int                      @logger()\n  \"database.password\": string &amp; !=\"\"          @secret() @logger()\n  \"api.secret_key\":  string &amp; !=\"\"            @secret()\n  max_connections:   int &amp; &gt;=1 &amp; &lt;=1000\n}\n</code></pre> <p>Register the schema in your bundle:</p> <pre><code>bundle: {\n  schema_registry: {\n    platform: \"github\"\n    repo:     \"my-org/schemas\"\n    branch:   \"main\"\n  }\n  // ...\n}\n</code></pre> <p>varTrack fetches the schema from Git, runs <code>cue vet</code> to validate the payload, then processes <code>@secret</code> and <code>@logger</code> annotations before writing to any sink.</p>"},{"location":"configuration/auth/","title":"Authentication &amp; Authorization","text":"<p>varTrack's CLI API (<code>/v1/cli/*</code>) is secured by a three-layer auth stack: OIDC for identity, RBAC for coarse-grained roles, and OPA for fine-grained policy. All three are optional \u2014 when absent, CLI routes are disabled.</p>"},{"location":"configuration/auth/#enabling-auth","title":"Enabling auth","text":"<p>Auth is configured in the bundle under <code>auth</code>:</p> <pre><code>bundle: {\n  auth: {\n    oidc: {\n      issuer_url: \"https://accounts.google.com\"\n      client_id:  \"my-client-id\"\n    }\n    rbac: {\n      policy: \"\"\"\n        p, role:admin,    *, *,         allow\n        p, role:operator, datasource, sync,     allow\n        p, role:operator, datasource, validate, allow\n        p, role:operator, task,       get,      allow\n        p, role:viewer,   datasource, validate, allow\n        p, role:viewer,   task,       get,      allow\n\n        g, alice@myorg.com, role:admin\n        g, bob@myorg.com,   role:operator\n      \"\"\"\n      default_role: \"role:viewer\"\n    }\n  }\n}\n</code></pre>"},{"location":"configuration/auth/#oidc-identity-layer","title":"OIDC \u2014 identity layer","text":"<p>varTrack validates the <code>Authorization: Bearer &lt;token&gt;</code> header against your OIDC provider. Any compliant provider works.</p> <pre><code>auth: {\n  oidc: {\n    issuer_url:   \"https://accounts.google.com\"  // required\n    client_id:    \"my-client-id\"                 // required\n    audience:     \"\"          // optional \u2014 defaults to client_id\n    extra_scopes: []          // optional additional OAuth2 scopes\n    groups_claim: \"groups\"    // JWT claim for group memberships\n  }\n}\n</code></pre> <p>Supported providers: Google, Azure AD / Entra ID, Okta, Auth0, Keycloak, GitLab, any OIDC-compliant provider.</p> <p>The CLI logs in via <code>vt login</code> and stores the token locally. The token is sent as a Bearer header on every request.</p>"},{"location":"configuration/auth/#rbac-role-layer-casbin","title":"RBAC \u2014 role layer (Casbin)","text":"<p>Casbin RBAC maps users and groups to roles, and roles to allowed actions.</p> <pre><code>auth: {\n  rbac: {\n    policy: \"\"\"\n      // Format: p, &lt;role&gt;, &lt;resource&gt;, &lt;action&gt;, allow\n      p, role:admin,    *,          *,        allow\n      p, role:operator, datasource, sync,     allow\n      p, role:operator, datasource, validate, allow\n      p, role:operator, task,       get,      allow\n      p, role:viewer,   datasource, validate, allow\n      p, role:viewer,   task,       get,      allow\n\n      // Format: g, &lt;email-or-group&gt;, &lt;role&gt;\n      g, alice@myorg.com,   role:admin\n      g, bob@myorg.com,     role:operator\n      g, platform-team,     role:operator    // group from groups_claim\n    \"\"\"\n    default_role: \"role:viewer\"              // for users with no explicit assignment\n  }\n}\n</code></pre> <p>Resources: <code>datasource</code>, <code>task</code>, <code>bundle</code>, <code>watcher</code></p> <p>Actions: <code>sync</code>, <code>validate</code>, <code>get</code>, <code>list</code>, <code>heal</code></p> <p>The <code>default_role</code> applies to any authenticated user who has no explicit role assignment.</p>"},{"location":"configuration/auth/#opa-policy-layer-open-policy-agent","title":"OPA \u2014 policy layer (Open Policy Agent)","text":"<p>OPA runs after RBAC for fine-grained decisions that RBAC can't express \u2014 e.g., \"only allow syncing to <code>production</code> if the user's group is <code>platform-team</code>\".</p> <pre><code>auth: {\n  opa: {\n    policy: \"\"\"\n      package vartrack.authz\n\n      default allow = false\n\n      allow {\n        input.action == \"sync\"\n        input.env    != \"production\"   // anyone can sync non-prod\n      }\n\n      allow {\n        input.action == \"sync\"\n        input.env    == \"production\"\n        \"platform-team\" in input.user.groups   // only platform-team can sync prod\n      }\n\n      allow {\n        input.action == \"validate\"     // everyone can validate\n      }\n    \"\"\"\n  }\n}\n</code></pre>"},{"location":"configuration/auth/#opa-input-document","title":"OPA input document","text":"<p>Every request evaluation receives:</p> Field Example <code>input.user.sub</code> <code>\"alice@myorg.com\"</code> <code>input.user.email</code> <code>\"alice@myorg.com\"</code> <code>input.user.groups</code> <code>[\"platform-team\", \"sre\"]</code> <code>input.action</code> <code>\"sync\"</code> <code>input.resource</code> <code>\"datasource\"</code> <code>input.datasource</code> <code>\"mongo\"</code> <code>input.env</code> <code>\"production\"</code> <code>input.file_path</code> <code>\"configs/app.yaml\"</code> <code>input.tenant_id</code> <code>\"acme\"</code> <code>input.dry_run</code> <code>false</code> <p>OPA runs embedded (in-process, ~50\u00b5s) \u2014 no external OPA server required. To reload a policy without restarting, call <code>POST /admin/opa/reload</code>.</p> <p>A full example policy is at <code>examples/policies/vartrack_authz.rego</code>.</p>"},{"location":"configuration/auth/#layer-evaluation-order","title":"Layer evaluation order","text":"<pre><code>Request\n  \u2502\n  \u25bc\nOIDC validation       \u2190 401 if token invalid or expired\n  \u2502\n  \u25bc\nRBAC check            \u2190 403 if role doesn't allow action\n  \u2502\n  \u25bc\nOPA evaluation        \u2190 403 if policy returns false\n  \u2502\n  \u25bc\nHandler\n</code></pre> <p>All three must pass. OPA is skipped if no <code>opa</code> block is configured.</p>"},{"location":"configuration/bundle/","title":"Bundle Reference","text":"<p>All three varTrack services read a single shared CUE bundle (<code>config.cue</code>). The bundle declares your platforms, datasources, routing rules, schema registry, and secret managers.</p>"},{"location":"configuration/bundle/#minimal-example","title":"Minimal example","text":"<pre><code>bundle: {\n  platforms: [{\n    github: {\n      endpoint:        \"https://github.com\"\n      push_event_name: \"push\"\n      pr_event_name:   \"pull_request\"\n      secret:          \"my-webhook-secret\"\n    }\n  }]\n\n  datasources: [{\n    mongo: {\n      endpoint:   \"mongodb://localhost:27017\"\n      database:   \"vartrack\"\n      collection: \"variables\"\n    }\n  }]\n\n  rules: [{\n    platform:             \"github\"\n    datasource:           \"mongo\"\n    file_name:            \"configs/app.yaml\"\n    repositories:         [\"my-org/*\"]\n    destination_template: \"{env}-config\"\n    self_heal:            true\n    branch_map: {\n      main:    \"production\"\n      develop: \"staging\"\n    }\n  }]\n}\n</code></pre>"},{"location":"configuration/bundle/#platforms","title":"<code>platforms</code>","text":"<p>Declares which Git platforms send webhooks to varTrack.</p>"},{"location":"configuration/bundle/#github","title":"GitHub","text":"<pre><code>platforms: [{\n  github: {\n    endpoint:        \"https://github.com\"     // or your GHE URL\n    protocol:        \"https\"                  // \"https\" or \"ssh\"\n    push_event_name: \"push\"\n    pr_event_name:   \"pull_request\"\n    secret:          \"my-secret\"              // webhook HMAC secret\n    token:           \"ghp_xxx\"               // PAT for cloning + repo listing\n    org_name:        \"my-org\"                 // optional \u2014 scope to one org\n    verify_ssl:      true\n    timeout:         \"30s\"\n  }\n}]\n</code></pre> <p>Multiple platform instances can be declared with different <code>tag</code> values:</p> <pre><code>platforms: [\n  { github: { tag: \"public\",     endpoint: \"https://github.com\", ... } },\n  { github: { tag: \"enterprise\", endpoint: \"https://git.mycompany.com\", ... } },\n]\n</code></pre>"},{"location":"configuration/bundle/#datasources","title":"<code>datasources</code>","text":"<p>Declares where config values should be written. Every datasource has an optional <code>tag</code> to distinguish multiple instances of the same type.</p>"},{"location":"configuration/bundle/#mongodb","title":"MongoDB","text":"<pre><code>datasources: [{\n  mongo: {\n    tag:             \"\"                        // optional \u2014 \"mongo-{tag}\" if set\n    endpoint:        \"mongodb://mongo:27017\"\n    database:        \"vartrack\"\n    collection:      \"variables\"\n    update_strategy: \"STRATEGY_KEY_VALUE\"      // or \"STRATEGY_DOCUMENT\"\n  }\n}]\n</code></pre>"},{"location":"configuration/bundle/#redis","title":"Redis","text":"<pre><code>datasources: [{\n  redis: {\n    host:     \"redis\"\n    port:     6379\n    password: \"secret\"\n    db:       0\n  }\n}]\n</code></pre>"},{"location":"configuration/bundle/#zookeeper","title":"ZooKeeper","text":"<pre><code>datasources: [{\n  zookeeper: {\n    hosts:    [\"zookeeper:2181\"]\n    base_path: \"/vartrack\"\n  }\n}]\n</code></pre>"},{"location":"configuration/bundle/#s3","title":"S3","text":"<pre><code>datasources: [{\n  s3: {\n    bucket:           \"my-config-bucket\"\n    region:           \"us-east-1\"\n    access_key_id:     \"AKIA...\"\n    secret_access_key: \"...\"\n  }\n}]\n</code></pre> <p>For all datasource options see the individual sink pages.</p>"},{"location":"configuration/bundle/#rules","title":"<code>rules</code>","text":"<p>Rules map a platform + file combination to a datasource and define how data is written.</p> <pre><code>rules: [{\n  platform:             \"github\"              // matches platform name (or \"github-{tag}\")\n  datasource:           \"mongo\"               // matches datasource name (or \"mongo-{tag}\")\n  file_name:            \"configs/app.yaml\"    // file path in the repo to watch\n  repositories:         [\"my-org/*\"]          // glob patterns \u2014 which repos trigger this\n\n  // Where data lands\n  destination_template: \"{env}-config\"\n\n  // Source (pick one)\n  file_name:     \"configs/app.yaml\"           // single file tracked in all repos\n  // file_path_map: { production: \"configs/prod.yaml\", staging: \"configs/staging.yaml\" }\n\n  // Repository filtering\n  exclude_repositories: [\"my-org/legacy-.*\"] // negative glob patterns\n  // repository_overrides: { \"my-org/special\": { enabled: false } }\n\n  // Root key extraction (optional)\n  root_key: \"vartrack\"                        // extract subtree before flattening\n                                              // set to \"\" to process the whole file\n\n  // Variable overlay (optional)\n  variables_map: {\n    REGION: \"us-east-1\"                       // inject or override any key\n  }\n\n  // Environment resolution\n  branch_map: {\n    main:    \"production\"\n    develop: \"staging\"\n  }\n  env_as_pr:     false\n  env_as_branch: false\n  env_as_tags:   false\n\n  // Sync behavior\n  sync_mode:     \"AUTO\"                       // AUTO, GIT_UPSERT_ALL, GIT_SMART_REPAIR, LIVE_STATE\n  prune:         false                        // delete keys no longer in Git\n  prune_last:    false                        // defer deletion until all sources processed\n  prune_protection: [\"SYSTEM_*\", \"_vt_*\"]    // glob patterns for keys that must never be deleted\n\n  // Drift detection\n  self_heal:     true                         // auto-repair on drift\n}]\n</code></pre>"},{"location":"configuration/bundle/#per-repo-overrides","title":"Per-repo overrides","text":"<p>Individual repositories can override a subset of their rule settings without touching <code>config.cue</code>. Add a <code>vartrack.json</code> file at the root of the repository:</p> <pre><code>{\n  \"root_key\": \"app_config\",\n  \"branch_map\": { \"main\": \"production\", \"staging\": \"staging\" },\n  \"prune\": true\n}\n</code></pre> <p>Keys in <code>vartrack.json</code> are merged on top of the central rule for that repository on every webhook. Infrastructure keys (<code>platform</code>, <code>datasource</code>, <code>token</code>, <code>repositories</code>, <code>self_heal</code>, \u2026) are always taken from the bundle and cannot be overridden. The schema registry is unaffected.</p> <p>See the full Per-Repo Overrides reference for the complete list of overridable keys, security model, and troubleshooting.</p>"},{"location":"configuration/bundle/#schema_registry","title":"<code>schema_registry</code>","text":"<p>Points varTrack to a Git repo containing CUE schemas for validation:</p> <pre><code>schema_registry: {\n  platform: \"github\"\n  repo:     \"my-org/schemas\"\n  branch:   \"main\"\n}\n</code></pre> <p>When a webhook arrives, varTrack validates the parsed config against the tenant's CUE schema before writing anything. Invalid payloads are rejected with a descriptive error.</p> <p>The schema repo also holds <code>bundle.json</code> (or <code>rules.json</code>) \u2014 a rule manifest that lets Celery workers resolve rule configuration locally without a round-trip to the API process. See the Schema Registry reference for the full repo structure, <code>bundle.json</code> format, tenant env vars, and caching behaviour.</p>"},{"location":"configuration/bundle/#secret_managers","title":"<code>secret_managers</code>","text":"<p>Secrets referenced in the bundle (tokens, passwords) can be resolved from HashiCorp Vault:</p> <pre><code>secret_managers: [{\n  vault: {\n    endpoint:    \"https://vault.mycompany.com\"\n    mount_point: \"secret\"\n    kv_version:  2\n\n    // Auth \u2014 pick one:\n    token_auth: {\n      token: \"hvs.xxxx\"\n    }\n    // approle_auth: {\n    //   role_id:   \"role-id\"\n    //   secret_id: \"secret-id\"\n    // }\n    // kubernetes_auth: {\n    //   role:            \"vartrack\"\n    //   service_account: \"/var/run/secrets/kubernetes.io/serviceaccount/token\"\n    // }\n    // userpass_auth: {\n    //   username: \"vartrack\"\n    //   password: \"pass\"\n    // }\n  }\n}]\n</code></pre>"},{"location":"configuration/bundle/#secret-value-forms","title":"Secret value forms","text":"<p>Every credential field in the bundle (<code>token</code>, <code>secret</code>, <code>password</code>, <code>private_key</code>, \u2026) accepts three forms:</p> <pre><code>// 1. Plain string \u2014 value written directly in the bundle\ntoken: \"ghp_xxx\"\n\n// 2. File reference \u2014 read from a file at service startup (whitespace stripped)\n//    Ideal for Docker secrets, Kubernetes secret mounts, or any on-disk secret\ntoken: {file: \"/run/secrets/github-token\"}\n\n// 3. Vault reference \u2014 fetched from HashiCorp Vault at ETL time\n//    Requires a secret_managers block in the bundle\ntoken: {ref: {path: \"vartrack/github\", key: \"token\"}}\n</code></pre>"},{"location":"configuration/bundle/#file-mounted-secret-files","title":"<code>{file:}</code> \u2014 mounted secret files","text":"<p>The <code>{file:}</code> form keeps secrets completely out of the bundle. Mount the file via Docker or Kubernetes and point to it:</p> <pre><code>secret_managers: [{\n  vault: {\n    endpoint:    \"https://vault.mycompany.com\"\n    mount_point: \"secret\"\n    kv_version:  2\n    token_auth: {\n      token: {file: \"/run/secrets/vault-token\"}   // Docker secret or K8s secret volume\n    }\n  }\n}]\n\nplatforms: [{\n  github: {\n    endpoint: \"https://github.com\"\n    token:    {file: \"/run/secrets/github-pat\"}\n    secret:   {file: \"/run/secrets/github-webhook-secret\"}\n  }\n}]\n</code></pre> <p>Docker Compose example:</p> <pre><code>services:\n  gateway:\n    image: vartrack/gateway\n    secrets:\n      - github-pat\n      - github-webhook-secret\n\nsecrets:\n  github-pat:\n    file: ./secrets/github-pat.txt\n  github-webhook-secret:\n    file: ./secrets/github-webhook-secret.txt\n</code></pre> <p>Kubernetes example:</p> <pre><code>apiVersion: v1\nkind: Secret\nmetadata:\n  name: vartrack-secrets\nstringData:\n  github-pat: ghp_xxx\n  github-webhook-secret: my-secret\n---\n# In the Pod spec:\nvolumeMounts:\n  - name: vartrack-secrets\n    mountPath: /run/secrets\n    readOnly: true\nvolumes:\n  - name: vartrack-secrets\n    secret:\n      secretName: vartrack-secrets\n</code></pre> <p>If the file cannot be read at startup, the service exits with a descriptive error.</p>"},{"location":"configuration/bundle/#global_tags","title":"<code>global_tags</code>","text":"<p>Arbitrary key-value metadata applied to every write across all sinks:</p> <pre><code>global_tags: {\n  team:        \"platform\"\n  environment: \"production\"\n}\n</code></pre>"},{"location":"configuration/bundle/#infrastructure-wiring","title":"Infrastructure wiring","text":"<p>Wire varTrack's internal components to specific datasources. Each value is the datasource name (<code>{type}</code> or <code>{type}-{tag}</code>):</p> <pre><code>bundle: {\n  celery_broker_datasource:            \"redis\"        // Celery task queue broker\n  celery_backend_datasource:           \"redis\"        // Celery result backend\n  gateway_nonce_datasource:            \"redis\"        // replay-protection nonce store\n  watcher_state_datasource:            \"redis\"        // watcher baseline state store\n  watcher_leader_election_datasource:  \"redis\"        // distributed leader election (Redis or ZooKeeper)\n}\n</code></pre> <p>In a single-Redis setup, all four can point to the same datasource. In production you may want separate instances for isolation. The <code>watcher_leader_election_datasource</code> also accepts <code>\"zookeeper\"</code>.</p> <p>See Drift Detection for how the watcher uses these.</p>"},{"location":"configuration/bundle/#gateway-tuning","title":"Gateway tuning","text":"<p>Fine-tune gateway-service behaviour directly from the bundle:</p>"},{"location":"configuration/bundle/#max_webhook_body_bytes","title":"<code>max_webhook_body_bytes</code>","text":"<p>Maximum size of an incoming webhook request body. Requests larger than this limit are rejected with HTTP 413. Defaults to 10 MiB when unset.</p> <pre><code>bundle: {\n  max_webhook_body_bytes: 5242880   // 5 MiB\n}\n</code></pre> Value Effect <code>0</code> or unset Use the default (10 MiB) Any positive integer Limit in bytes <p>Raise this if you receive large monorepo push payloads. Lower it to reduce memory pressure on high-traffic gateways.</p>"},{"location":"configuration/formats/","title":"Supported File Formats","text":"<p>varTrack auto-detects the format of your config file from its extension. All formats are parsed into the same flat <code>{ \"key\": \"value\" }</code> map before writing to any sink.</p>"},{"location":"configuration/formats/#supported-formats","title":"Supported formats","text":"Extension Format Notes <code>.yaml</code>, <code>.yml</code> YAML Nested keys flattened with dot notation <code>.json</code> JSON Nested keys flattened with dot notation <code>.toml</code> TOML Sections become key prefixes <code>.env</code> dotenv / KEY=VALUE One <code>KEY=value</code> per line <code>.ini</code> INI Section + key become <code>section.key</code> <code>.hcl</code> HCL Terraform-style config files <code>.xml</code> XML Elements become dot-separated keys <p>Use <code>--format</code> on the <code>vt sync</code> command to override auto-detection.</p>"},{"location":"configuration/formats/#examples","title":"Examples","text":"YAMLJSON.envTOMLINIHCL <pre><code># configs/app.yaml\ndatabase:\n  host: mongo.prod.internal\n  port: 27017\n  name: myapp\n\nfeature_flags:\n  dark_mode: true\n  beta_ui: false\n\nmax_connections: 50\n</code></pre> <p>Flattened result:</p> <pre><code>database.host         = \"mongo.prod.internal\"\ndatabase.port         = \"27017\"\ndatabase.name         = \"myapp\"\nfeature_flags.dark_mode = \"true\"\nfeature_flags.beta_ui   = \"false\"\nmax_connections         = \"50\"\n</code></pre> <pre><code>{\n  \"database\": {\n    \"host\": \"mongo.prod.internal\",\n    \"port\": 27017\n  },\n  \"max_connections\": 50\n}\n</code></pre> <p>Flattened result:</p> <pre><code>database.host    = \"mongo.prod.internal\"\ndatabase.port    = \"27017\"\nmax_connections  = \"50\"\n</code></pre> <pre><code># configs/app.env\nDATABASE_HOST=mongo.prod.internal\nDATABASE_PORT=27017\nMAX_CONNECTIONS=50\nFEATURE_DARK_MODE=true\n</code></pre> <p>Flattened result (keys preserved as-is):</p> <pre><code>DATABASE_HOST       = \"mongo.prod.internal\"\nDATABASE_PORT       = \"27017\"\nMAX_CONNECTIONS     = \"50\"\nFEATURE_DARK_MODE   = \"true\"\n</code></pre> <pre><code>[database]\nhost = \"mongo.prod.internal\"\nport = 27017\n\n[feature_flags]\ndark_mode = true\n</code></pre> <p>Flattened result:</p> <pre><code>database.host          = \"mongo.prod.internal\"\ndatabase.port          = \"27017\"\nfeature_flags.dark_mode = \"true\"\n</code></pre> <pre><code>[database]\nhost = mongo.prod.internal\nport = 27017\n\n[app]\nmax_connections = 50\n</code></pre> <p>Flattened result:</p> <pre><code>database.host    = \"mongo.prod.internal\"\ndatabase.port    = \"27017\"\napp.max_connections = \"50\"\n</code></pre> <pre><code>database {\n  host = \"mongo.prod.internal\"\n  port = 27017\n}\n\nmax_connections = 50\n</code></pre> <p>Flattened result:</p> <pre><code>database.host    = \"mongo.prod.internal\"\ndatabase.port    = \"27017\"\nmax_connections  = \"50\"\n</code></pre>"},{"location":"configuration/formats/#root-key-slicing","title":"Root key slicing","text":"<p>If your config file uses a <code>vartrack</code> top-level key, varTrack extracts only that subtree:</p> <pre><code># Other top-level keys are ignored\napp_name: my-service\nversion: 2.3.1\n\nvartrack:\n  database_host: mongo.prod.internal\n  max_connections: 50\n</code></pre> <p>Only <code>database_host</code> and <code>max_connections</code> are written. This lets you keep varTrack config alongside other metadata in the same file.</p>"},{"location":"configuration/formats/#dry-run-preview-what-would-be-written","title":"Dry-run \u2014 preview what would be written","text":"<p>Before any real sync, you can simulate the full parse \u2192 flatten \u2192 env-slice pipeline without writing anything to a sink:</p> <pre><code># CLI\nvt sync --file configs/app.yaml --datasource mongo --env staging --dry-run\n\n# HTTP\nPOST /v1/webhooks/mongo/dry-run\n</code></pre> <p>The response shows the exact flat key/value map that would be written \u2014 after root_key extraction, env-slicing, and <code>@secret</code> masking. Nothing touches the datasource.</p> <p>This is especially useful when introducing a new file format or env pattern: run dry-run first to confirm varTrack is parsing and slicing the file exactly as expected before merging to <code>main</code>.</p>"},{"location":"configuration/formats/#environment-key-patterns","title":"Environment key patterns","text":"<p>varTrack auto-detects three patterns for environment-keyed config files. No rule config is needed \u2014 the structure is inferred automatically.</p>"},{"location":"configuration/formats/#pattern-1-all-top-level-keys-are-env-names","title":"Pattern 1 \u2014 all top-level keys are env names","text":"<p>Every top-level value is a dict. varTrack merges <code>default</code> first, then applies the matching env as an override.</p> <pre><code># configs/app.yaml\nproduction:\n  database_host: mongo.prod.internal\n  max_connections: 100\n  log_level: warn\n\nstaging:\n  database_host: mongo.staging.internal\n  max_connections: 10\n  log_level: info\n\ndefault:\n  max_connections: 5      # fallback for any unknown env\n  log_level: debug\n  cache_ttl: 60\n</code></pre> <p>Push to <code>main</code> (resolved env = <code>production</code>):</p> <pre><code>database_host   = \"mongo.prod.internal\"\nmax_connections = \"100\"\nlog_level       = \"warn\"\ncache_ttl       = \"60\"           \u2190 inherited from default\n</code></pre> <p>Push from PR #42 (env = <code>pr-42</code>, no match \u2192 <code>default</code>):</p> <pre><code>max_connections = \"5\"\nlog_level       = \"debug\"\ncache_ttl       = \"60\"\n</code></pre>"},{"location":"configuration/formats/#pattern-2-per-field-env-sub-keys-mixed-with-scalars","title":"Pattern 2 \u2014 per-field env sub-keys mixed with scalars","text":"<p>Each field that varies per env has a dict of <code>env \u2192 value</code>. Fields that are the same across all envs are plain scalars \u2014 they are written as-is for every env.</p> <pre><code># configs/app.yaml\ndatabase_host:\n  production: mongo.prod.internal\n  staging:    mongo.staging.internal\n  default:    mongo.local\n\nmax_connections: 50        # same for every env\nlog_level: info            # same for every env\ncache_ttl:\n  production: 300\n  staging:    60\n  default:    10\n</code></pre> <p>Push to <code>main</code> (env = <code>production</code>):</p> <pre><code>database_host   = \"mongo.prod.internal\"\nmax_connections = \"50\"\nlog_level       = \"info\"\ncache_ttl       = \"300\"\n</code></pre> <p>Push to <code>develop</code> (env = <code>staging</code>):</p> <pre><code>database_host   = \"mongo.staging.internal\"\nmax_connections = \"50\"\nlog_level       = \"info\"\ncache_ttl       = \"60\"\n</code></pre>"},{"location":"configuration/formats/#pattern-3-env-dicts-and-scalar-defaults-at-the-same-level","title":"Pattern 3 \u2014 env dicts and scalar defaults at the same level","text":"<p>Some keys at the same nesting level are env names (dicts with overrides), others are plain scalars that apply to every env. Order in the file doesn't matter \u2014 varTrack categorizes by value type, not position. The env dict is merged on top of the scalars.</p> <pre><code>{\n  \"production\": { \"database_host\": \"mongo.prod.internal\", \"log_level\": \"warn\"  },\n  \"staging\":    { \"database_host\": \"mongo.stg.internal\",  \"log_level\": \"info\"  },\n  \"default\":    { \"database_host\": \"mongo.local\",         \"log_level\": \"debug\" },\n  \"max_connections\": 50,\n  \"cache_ttl\":       60,\n  \"app_name\":        \"my-service\"\n}\n</code></pre> <p>Push to <code>main</code> (env = <code>production</code>):</p> <pre><code>database_host   = \"mongo.prod.internal\"   \u2190 from production dict\nlog_level       = \"warn\"                  \u2190 from production dict\nmax_connections = \"50\"                    \u2190 from scalar\ncache_ttl       = \"60\"                    \u2190 from scalar\napp_name        = \"my-service\"            \u2190 from scalar\n</code></pre> <p>Push from PR #42 (env = <code>pr-42</code>, no match \u2192 <code>default</code> dict):</p> <pre><code>database_host   = \"mongo.local\"\nlog_level       = \"debug\"\nmax_connections = \"50\"\ncache_ttl       = \"60\"\napp_name        = \"my-service\"\n</code></pre> <p>If there is no <code>default</code> dict and no matching env dict, data is returned unchanged.</p>"},{"location":"configuration/formats/#combining-patterns-with-root_key","title":"Combining patterns with <code>root_key</code>","text":"<p>All three patterns work after <code>root_key</code> extraction. This lets you embed varTrack config inside files owned by other tools:</p> <pre><code># package.json style \u2014 but in YAML for readability\nname: my-app\nversion: 1.0.0\n\nvartrack:\n  production: { API_URL: \"https://api.prod.com\", LOG_LEVEL: warn  }\n  staging:    { API_URL: \"https://api.stg.com\",  LOG_LEVEL: info  }\n  default:    { API_URL: \"https://api.local.com\", LOG_LEVEL: debug }\n  TIMEOUT: 30\n</code></pre> <pre><code>rules: [{\n  file_name: \"config.yaml\"\n  root_key:  \"vartrack\"      // extract the vartrack block first, then detect pattern\n  branch_map: { main: \"production\", develop: \"staging\" }\n}]\n</code></pre> <p>Push to <code>main</code> \u2192 extracts <code>vartrack</code> subtree \u2192 detects Pattern 3 \u2192 writes <code>API_URL=https://api.prod.com</code>, <code>LOG_LEVEL=warn</code>, <code>TIMEOUT=30</code>.</p>"},{"location":"configuration/per-repo-overrides/","title":"Per-Repo Overrides (<code>vartrack.json</code>)","text":"<p>Individual repositories can override a subset of their central rule configuration without touching <code>config.cue</code>. Place a <code>vartrack.json</code> file at the root of the repository and varTrack will apply its settings on top of the matching rule whenever a webhook is received.</p>"},{"location":"configuration/per-repo-overrides/#how-it-works","title":"How it works","text":"<p>When a push webhook arrives, the ETL pipeline:</p> <ol> <li>Resolves the central rule from <code>config.cue</code> (Stage 1 \u2014 Payload)</li> <li>Fetches <code>vartrack.json</code> from the repository at the pushed commit ref</li> <li>Merges the overridable keys from <code>vartrack.json</code> into the active rule config</li> <li>Runs ETL and Sync with the merged settings (Stages 2 and 3)</li> </ol> <p>If <code>vartrack.json</code> is absent, malformed, or empty, the central rule is used unchanged. The schema registry is never affected by per-repo overrides.</p>"},{"location":"configuration/per-repo-overrides/#example","title":"Example","text":"<pre><code>{\n  \"root_key\": \"app_config\",\n  \"branch_map\": {\n    \"main\": \"production\",\n    \"staging\": \"staging\",\n    \"develop\": \"develop\"\n  },\n  \"prune\": true,\n  \"sync_mode\": \"GIT_UPSERT_ALL\"\n}\n</code></pre> <p>Place this file at the root of the repository (next to your config file):</p> <pre><code>my-repo/\n\u251c\u2500\u2500 vartrack.json        \u2190 per-repo overrides\n\u2514\u2500\u2500 configs/\n    \u2514\u2500\u2500 app.yaml         \u2190 your config file (watched by the central rule)\n</code></pre>"},{"location":"configuration/per-repo-overrides/#overridable-keys","title":"Overridable keys","text":"<p>These keys can be set in <code>vartrack.json</code> to override the central rule:</p> Key Type Description <code>root_key</code> <code>string</code> Subtree to extract before flattening. <code>\"\"</code> processes the whole file. <code>file_name</code> <code>string</code> Config file path to watch instead of the centrally configured one. <code>file_path_map</code> <code>object</code> Map of <code>env \u2192 file_path</code> for per-environment file selection. <code>branch_map</code> <code>object</code> Map of <code>branch \u2192 env</code> label. Overrides the central branch mapping. <code>env_as_branch</code> <code>bool</code> Use branch name as the environment label. <code>env_as_pr</code> <code>bool</code> Use <code>pr-{number}</code> as the environment label. <code>env_as_tags</code> <code>bool</code> Use tag name as the environment label. <code>sync_mode</code> <code>string</code> <code>AUTO</code>, <code>GIT_UPSERT_ALL</code>, <code>GIT_SMART_REPAIR</code>, or <code>LIVE_STATE</code>. <code>prune</code> <code>bool</code> Delete keys from the datasource that are no longer present in Git. <code>strict_validation</code> <code>bool</code> Fail the sync if CUE schema validation fails (instead of warn). <code>variables_map</code> <code>object</code> Key-value pairs injected or overridden after ETL transform. <code>apply_strategy</code> <code>int</code> Internal apply strategy (advanced). <code>dry_run</code> <code>bool</code> Permanently shadow this repo \u2014 Stage 3 never writes anything."},{"location":"configuration/per-repo-overrides/#security-model","title":"Security model","text":"<p>Infrastructure and security-critical keys are always taken from the central bundle and cannot be changed by the repository:</p> <ul> <li><code>platform</code>, <code>datasource</code>, <code>repo_url</code>, <code>tenant_id</code></li> <li><code>token</code>, <code>secrets</code>, <code>secret_managers</code></li> <li><code>repositories</code>, <code>exclude_repositories</code></li> <li><code>self_heal</code> and all Celery wiring keys</li> </ul> <p>Any key in <code>vartrack.json</code> that is not in the overridable list above is silently ignored and logged at <code>INFO</code> level.</p>"},{"location":"configuration/per-repo-overrides/#interaction-with-the-schema-registry","title":"Interaction with the schema registry","text":"<p><code>vartrack.json</code> has no effect on CUE schema validation. The schema used to validate your config file is always sourced from the tenant's schema registry (configured in <code>config.cue</code> under <code>schema_registry</code>). Per-repo overrides cannot change which schema is applied or bypass validation.</p>"},{"location":"configuration/per-repo-overrides/#troubleshooting","title":"Troubleshooting","text":"<p>Override not applied \u2014 Check the orchestrator logs for: <pre><code>repo_overrides: no vartrack.json repo=... ref=...\n</code></pre> This means the file was not found at the repo root for that commit ref.</p> <p>Keys ignored \u2014 If you see: <pre><code>repo_overrides: ignoring non-overridable keys ['platform', ...] repo=...\n</code></pre> Those keys are infrastructure-level and cannot be overridden from the repo.</p> <p>Invalid JSON \u2014 A parse error produces: <pre><code>repo_overrides: invalid JSON in vartrack.json repo=... ref=...\n</code></pre> The central rule is used unchanged.</p>"},{"location":"configuration/schema-registry/","title":"Schema Registry","text":"<p>The schema registry is a Git repository that holds two things:</p> <ol> <li>CUE schema files \u2014 used to validate config files before any write</li> <li><code>bundle.json</code> / <code>rules.json</code> \u2014 a rule manifest that lets Celery workers resolve rules locally without a round-trip to the API process</li> </ol>"},{"location":"configuration/schema-registry/#repo-structure","title":"Repo structure","text":"<pre><code>my-org/schemas/                  \u2190 schema registry repo\n\u251c\u2500\u2500 bundle.json                  \u2190 rule manifest (optional but recommended)\n\u251c\u2500\u2500 app-settings.yaml.cue        \u2190 CUE schema for app-settings.yaml\n\u251c\u2500\u2500 config.toml.cue              \u2190 CUE schema for config.toml\n\u251c\u2500\u2500 dotenv.cue                   \u2190 CUE schema for .env files\n\u2514\u2500\u2500 default.cue                  \u2190 fallback schema when no file-specific match\n</code></pre> <p>The schema repo is pointed to from <code>config.cue</code>:</p> <pre><code>schema_registry: {\n  platform: \"github\"\n  repo:     \"my-org/schemas\"\n  branch:   \"main\"\n}\n</code></pre> <p>varTrack clones this repo for each tenant on first use and refreshes it automatically on a TTL (default 5 minutes) or immediately when the schema repo itself receives a push webhook.</p>"},{"location":"configuration/schema-registry/#bundlejson","title":"<code>bundle.json</code>","text":"<p><code>bundle.json</code> is the primary rule manifest. It is a JSON array of rule objects \u2014 one entry per platform + datasource combination the tenant uses.</p> <p>Workers parse it on startup to resolve <code>rule_config</code> locally, avoiding a round-trip to the orchestrator API process on every task.</p>"},{"location":"configuration/schema-registry/#format","title":"Format","text":"<p>Two formats are accepted:</p> <p>Array (recommended): <pre><code>[\n  { ... rule object ... },\n  { ... rule object ... }\n]\n</code></pre></p> <p>Object with a <code>rules</code> key: <pre><code>{\n  \"rules\": [\n    { ... rule object ... }\n  ]\n}\n</code></pre></p>"},{"location":"configuration/schema-registry/#full-example","title":"Full example","text":"<pre><code>[\n  {\n    \"platform\":         \"github\",\n    \"datasource\":       \"mongo\",\n    \"tenant_id\":        \"acme\",\n    \"repo_url\":         \"https://github.com/acme/app-configs.git\",\n    \"file_name\":        \"configs/app.yaml\",\n    \"repositories\":     [\"acme/*\"],\n    \"branch\":           \"main\",\n    \"branch_map\": {\n      \"main\":    \"production\",\n      \"develop\": \"staging\"\n    },\n    \"sync_mode\":        \"SYNC_MODE_FULL\",\n    \"prune\":            true,\n    \"prune_last\":       false,\n    \"self_heal\":        true,\n    \"root_key\":         \"vartrack\",\n    \"mongo_uri\":        \"mongodb://mongo:27017\",\n    \"database\":         \"vartrack\",\n    \"collection\":       \"variables\",\n    \"update_strategy\":  \"STRATEGY_KEY_VALUE\"\n  },\n  {\n    \"platform\":         \"github\",\n    \"datasource\":       \"redis\",\n    \"tenant_id\":        \"acme\",\n    \"repo_url\":         \"https://github.com/acme/app-configs.git\",\n    \"file_name\":        \"configs/app.yaml\",\n    \"repositories\":     [\"acme/*\"],\n    \"branch\":           \"main\",\n    \"sync_mode\":        \"SYNC_MODE_FULL\",\n    \"prune\":            true,\n    \"self_heal\":        true,\n    \"redis_host\":       \"redis\",\n    \"redis_port\":       6379,\n    \"redis_db\":         0\n  }\n]\n</code></pre>"},{"location":"configuration/schema-registry/#rule-object-fields","title":"Rule object fields","text":"Field Required Description <code>platform</code> Yes Platform name \u2014 <code>github</code>, <code>gitlab</code>, <code>gitea</code>, or <code>github-{tag}</code> <code>datasource</code> Yes Datasource name \u2014 <code>mongo</code>, <code>redis</code>, <code>zookeeper</code>, <code>s3</code>, etc. <code>tenant_id</code> Yes Tenant this rule belongs to <code>repo_url</code> Yes HTTPS clone URL of the config repository <code>file_name</code> One of Single file path to track in all repos <code>file_path_map</code> One of <code>{ env \u2192 file_path }</code> for per-environment file selection <code>repositories</code> No Glob patterns for which repos trigger this rule <code>branch</code> No Branch to track (default: <code>main</code>) <code>branch_map</code> No <code>{ branch \u2192 env }</code> label mapping <code>sync_mode</code> No <code>AUTO</code>, <code>SYNC_MODE_FULL</code>, <code>GIT_UPSERT_ALL</code>, <code>GIT_SMART_REPAIR</code>, <code>LIVE_STATE</code> <code>prune</code> No Delete datasource keys no longer in Git (default: <code>false</code>) <code>prune_last</code> No Defer key deletion until all sources are processed <code>self_heal</code> No Auto-restore on drift (default: <code>false</code>) <code>root_key</code> No Subtree key to extract before flattening (default: <code>\"vartrack\"</code>) <code>env_as_branch</code> No Use branch name as the environment label <code>env_as_pr</code> No Use <code>pr-{number}</code> as the environment label <code>env_as_tags</code> No Use tag name as the environment label <p>Additional sink-specific fields (connection strings, credentials, etc.) are included inline per rule \u2014 see the individual sink pages for what each datasource type accepts.</p>"},{"location":"configuration/schema-registry/#rulesjson","title":"<code>rules.json</code>","text":"<p><code>rules.json</code> is a drop-in alias for <code>bundle.json</code>. If both files exist, <code>bundle.json</code> takes precedence. Use <code>rules.json</code> if you prefer a shorter name or want to distinguish the manifest from other bundle exports.</p> <pre><code>my-org/schemas/\n\u251c\u2500\u2500 rules.json     \u2190 identical format to bundle.json\n\u2514\u2500\u2500 *.cue\n</code></pre>"},{"location":"configuration/schema-registry/#tenant-configuration","title":"Tenant configuration","text":"<p>Tenants are registered via environment variables on the orchestrator-service and its Celery workers:</p> <pre><code>SCHEMA_TENANT_ACME_REPO=https://github.com/acme/schemas.git\nSCHEMA_TENANT_ACME_BRANCH=main\nSCHEMA_TENANT_ACME_TOKEN=ghp_xxx\n</code></pre> <p>The tenant ID is derived from the variable name (<code>ACME</code> \u2192 <code>acme</code>). Multiple tenants can be registered in the same process:</p> <pre><code>SCHEMA_TENANT_ACME_REPO=https://github.com/acme/schemas.git\nSCHEMA_TENANT_ACME_TOKEN=ghp_xxx\n\nSCHEMA_TENANT_BETA_REPO=https://github.com/beta-corp/schemas.git\nSCHEMA_TENANT_BETA_BRANCH=production\nSCHEMA_TENANT_BETA_TOKEN=ghp_yyy\n</code></pre>"},{"location":"configuration/schema-registry/#caching-and-refresh","title":"Caching and refresh","text":"Behaviour Detail Clone on first use Cloned to <code>SCHEMA_CACHE_DIR</code> (default <code>/tmp/schema_registry</code>) TTL refresh Re-fetched after <code>SCHEMA_TTL_SECONDS</code> (default 300 s) Webhook invalidation A push to the schema repo triggers an immediate re-clone for that tenant Worker disk fallback Celery workers read from the shared <code>SCHEMA_CACHE_DIR</code> volume without needing their own warm-up Rule resolution cache Resolved rules are cached in Redis for 300 s under key <code>rule:{tenant_id}:{platform}:{datasource}</code> <p>To share the clone cache between the API process and workers in Docker Compose or Kubernetes, mount <code>SCHEMA_CACHE_DIR</code> as a shared volume.</p>"},{"location":"configuration/schema-registry/#relationship-to-configcue","title":"Relationship to <code>config.cue</code>","text":"<p><code>config.cue</code> is the authoritative source for which tenants, platforms, and datasources exist. <code>bundle.json</code> is a worker-side cache of the relevant rules, formatted as flat JSON so workers can resolve <code>rule_config</code> without deserialising the full CUE bundle on every task.</p> <p>The two should be kept in sync. When you add or change a rule in <code>config.cue</code>, update <code>bundle.json</code> in the schema repo and push \u2014 the schema webhook will cause workers to pick up the new rules within seconds.</p>"},{"location":"deployment/docker-compose/","title":"Docker Compose Quickstart","text":"<p>The fastest way to run varTrack in production is with the provided <code>docker-compose.yml</code> at the repo root. It uses pre-built images from Docker Hub and starts the complete stack \u2014 gateway, orchestrator, worker, watcher, MongoDB, and Redis \u2014 with a single command.</p>"},{"location":"deployment/docker-compose/#prerequisites","title":"Prerequisites","text":"<ul> <li>Docker 24+ and Docker Compose v2</li> <li>A <code>config.cue</code> bundle (see Bundle Reference)</li> <li>A GitHub (or GitLab / Gitea) repository with a webhook pointing at your server</li> </ul>"},{"location":"deployment/docker-compose/#1-configure","title":"1. Configure","text":"<p>Copy the example environment file and fill in your values:</p> <pre><code>cp .env.example .env\n</code></pre> <p>The only required change is <code>MONGO_ROOT_PASSWORD</code>. Everything else has a sensible default:</p> <pre><code># .env \u2014 minimum required\nMONGO_ROOT_PASSWORD=your-strong-password\n</code></pre> <p>Full <code>.env</code> reference:</p> Variable Default Description <code>CONFIG_PATH</code> <code>./config.cue</code> Path to your CUE bundle <code>APP_ENV</code> <code>production</code> <code>production</code> enables mTLS; <code>development</code> is permissive <code>LOG_LEVEL</code> <code>INFO</code> <code>DEBUG</code>, <code>INFO</code>, <code>WARNING</code>, <code>ERROR</code> <code>MONGO_ROOT_USER</code> <code>vartrack</code> MongoDB root username <code>MONGO_ROOT_PASSWORD</code> (required) MongoDB root password <code>GATEWAY_PORT</code> <code>5657</code> Host port for the webhook receiver <code>ORCHESTRATOR_PORT</code> <code>8000</code> Host port for the orchestrator HTTP API <code>WORKER_CONCURRENCY</code> <code>4</code> Celery worker concurrency <code>WATCHER_POLL_INTERVAL</code> <code>60s</code> How often the watcher checks for drift <code>CORS_ORIGINS</code> (empty) Browser origin for CORS headers (optional)"},{"location":"deployment/docker-compose/#2-write-your-configcue","title":"2. Write your <code>config.cue</code>","text":"<p>Place <code>config.cue</code> next to <code>docker-compose.yml</code> (or set <code>CONFIG_PATH</code> to point elsewhere). Minimal example with GitHub \u2192 MongoDB:</p> <pre><code>bundle: {\n  platforms: [{\n    github: {\n      endpoint:        \"https://github.com\"\n      push_event_name: \"push\"\n      pr_event_name:   \"pull_request\"\n      secret:          \"your-webhook-secret\"   // HMAC secret set in GitHub\n      token:           \"ghp_xxx\"               // PAT for cloning private repos\n    }\n  }]\n\n  datasources: [{\n    mongo: {\n      endpoint:   \"mongodb://vartrack:your-strong-password@mongo:27017\"\n      database:   \"vartrack\"\n      collection: \"variables\"\n    }\n  }]\n\n  rules: [{\n    platform:             \"github\"\n    datasource:           \"mongo\"\n    file_name:            \"configs/app.yaml\"\n    repositories:         [\"my-org/*\"]\n    destination_template: \"{env}-config\"\n    self_heal:            true\n    branch_map: {\n      main:    \"production\"\n      develop: \"staging\"\n    }\n  }]\n\n  celery_broker_datasource:   \"redis\"\n  celery_backend_datasource:  \"redis\"\n  watcher_state_datasource:   \"redis\"\n\n  datasources: [{\n    redis: {\n      host: \"redis\"\n      port: 6379\n      db:   0\n    }\n  }]\n}\n</code></pre> <p>See the Bundle Reference for all platforms, datasources, and rule options.</p>"},{"location":"deployment/docker-compose/#3-start-the-stack","title":"3. Start the stack","text":"<pre><code>docker compose up -d\n</code></pre> <p>Check that all services are healthy:</p> <pre><code>docker compose ps\n</code></pre> <p>Expected output:</p> <pre><code>NAME                    STATUS\nvartrack-redis-1        running (healthy)\nvartrack-mongo-1        running (healthy)\nvartrack-orchestrator-api-1    running (healthy)\nvartrack-orchestrator-worker-1 running\nvartrack-gateway-1      running (healthy)\nvartrack-watcher-1      running (healthy)\n</code></pre>"},{"location":"deployment/docker-compose/#4-register-the-webhook-in-github","title":"4. Register the webhook in GitHub","text":"<p>In your GitHub repository go to Settings \u2192 Webhooks \u2192 Add webhook:</p> Field Value Payload URL <code>http://your-server:5657/webhooks/mongo</code> Content type <code>application/json</code> Secret The <code>secret</code> value from your <code>config.cue</code> Events Pushes and Pull requests <p>Replace <code>mongo</code> in the URL with your datasource name (<code>redis</code>, <code>s3</code>, <code>configmap</code>, etc.) if you use a different sink.</p> <p>Push a change to your config file \u2014 varTrack will sync it automatically.</p>"},{"location":"deployment/docker-compose/#useful-commands","title":"Useful commands","text":"<pre><code># Follow all logs\ndocker compose logs -f\n\n# Follow a single service\ndocker compose logs -f orchestrator-worker\n\n# Restart a single service\ndocker compose restart gateway\n\n# Stop everything (keeps volumes)\ndocker compose down\n\n# Stop and delete all data\ndocker compose down -v\n</code></pre>"},{"location":"deployment/docker-compose/#checking-task-results","title":"Checking task results","text":"<p>The orchestrator API exposes task status at port 8000:</p> <pre><code># OpenAPI docs\nopen http://localhost:8000/docs\n\n# Check a specific task\ncurl http://localhost:8000/v1/tasks/&lt;task-id&gt;\n\n# Prometheus metrics\ncurl http://localhost:8000/metrics\n</code></pre>"},{"location":"deployment/docker-compose/#using-an-external-mongodb","title":"Using an external MongoDB","text":"<p>If you already have a MongoDB, remove the <code>mongo</code> service and volume from <code>docker-compose.yml</code> and update <code>config.cue</code> to point at your connection string:</p> <pre><code>datasources: [{\n  mongo: {\n    endpoint:   \"mongodb://user:pass@your-mongo-host:27017\"\n    database:   \"vartrack\"\n    collection: \"variables\"\n  }\n}]\n</code></pre>"},{"location":"deployment/docker-compose/#scaling-workers","title":"Scaling workers","text":"<p>Run more Celery workers to handle higher webhook throughput:</p> <pre><code>docker compose up -d --scale orchestrator-worker=3\n</code></pre> <p>Workers share the <code>schema-cache</code> and <code>git-cache</code> volumes so clones are reused across replicas.</p>"},{"location":"deployment/docker-compose/#disabling-drift-detection","title":"Disabling drift detection","text":"<p>If you don't need self-heal, remove the <code>watcher</code> service from <code>docker-compose.yml</code> and omit <code>self_heal: true</code> from your rules. This reduces resource usage with no effect on the sync pipeline.</p>"},{"location":"examples/","title":"Examples","text":"<p>Complete bundle examples for common scenarios. All examples are also available in the <code>examples/bundles/</code> directory.</p>"},{"location":"examples/#mongodb-basic","title":"MongoDB basic","text":"<p>Push config from a GitHub repo to MongoDB. Branch name maps to environment.</p> <pre><code>bundle: {\n  platforms: [{\n    github: {\n      endpoint:        \"https://github.com\"\n      push_event_name: \"push\"\n      pr_event_name:   \"pull_request\"\n      secret:          \"my-webhook-secret\"\n      token:           \"ghp_xxx\"\n    }\n  }]\n\n  datasources: [{\n    mongo: {\n      endpoint:   \"mongodb://mongo:27017\"\n      database:   \"vartrack\"\n      collection: \"variables\"\n    }\n  }]\n\n  rules: [{\n    platform:             \"github\"\n    datasource:           \"mongo\"\n    file_name:            \"configs/app.yaml\"\n    repositories:         [\"my-org/*\"]\n    destination_template: \"{env}-config\"\n    self_heal:            true\n    branch_map: {\n      main:    \"production\"\n      develop: \"staging\"\n    }\n  }]\n}\n</code></pre>"},{"location":"examples/#config-file-in-git","title":"Config file in Git","text":"<p>The <code>file_name</code> in a rule points to a file inside your Git repository. varTrack parses it and writes every key-value pair to the sink. Example file at <code>configs/app.yaml</code>:</p> <pre><code># configs/app.yaml\napp_name:        my-service\nlog_level:       info\nmax_connections: 100\ndebug:           false\n</code></pre> <p>When pushed to <code>main</code>, varTrack writes all four keys to the MongoDB collection <code>production-config</code>:</p> Key Value <code>app_name</code> <code>my-service</code> <code>log_level</code> <code>info</code> <code>max_connections</code> <code>100</code> <code>debug</code> <code>false</code>"},{"location":"examples/#nested-keys","title":"Nested keys","text":"<p>Nested YAML/JSON objects are flattened with dot notation:</p> <pre><code>database:\n  host: prod-db.internal\n  port: 5432\n  name: myapp\n\nserver:\n  port: 8080\n  timeout: 30\n</code></pre> <p>Written to the sink as: <code>database.host</code>, <code>database.port</code>, <code>database.name</code>, <code>server.port</code>, <code>server.timeout</code>.</p>"},{"location":"examples/#files-that-contain-more-than-just-config","title":"Files that contain more than just config","text":"<p>Real-world config files often carry metadata, version info, or multiple services in the same file \u2014 things you don't want to push to your sink. Use <code>root_key</code> to extract only the relevant subtree before varTrack processes the file.</p>"},{"location":"examples/#packagejson-extract-only-the-vartrack-block","title":"<code>package.json</code> \u2014 extract only the vartrack block","text":"<p>A Node.js project uses <code>package.json</code> as its canonical config store. The file has <code>name</code>, <code>version</code>, <code>scripts</code>, <code>dependencies</code>, etc. \u2014 but varTrack should only sync the <code>vartrack</code> block:</p> <pre><code>{\n  \"name\": \"my-service\",\n  \"version\": \"2.4.1\",\n  \"scripts\": { \"start\": \"node index.js\" },\n  \"dependencies\": { \"express\": \"^4.18.0\" },\n  \"vartrack\": {\n    \"database_host\": \"mongo.prod.internal\",\n    \"max_connections\": 50,\n    \"feature_dark_mode\": true\n  }\n}\n</code></pre> <pre><code>rules: [{\n  file_name: \"package.json\"\n  root_key:  \"vartrack\"    // only the inner object is flattened and written\n}]\n</code></pre> <p>Keys written: <code>database_host</code>, <code>max_connections</code>, <code>feature_dark_mode</code>. Everything outside the <code>vartrack</code> block is ignored.</p>"},{"location":"examples/#multi-service-monorepo-file-extract-one-services-block","title":"Multi-service monorepo file \u2014 extract one service's block","text":"<p>A single <code>configs/services.yaml</code> declares config for several services:</p> <pre><code>auth-service:\n  jwt_secret:      \"changeme\"\n  token_ttl:       3600\n  refresh_enabled: true\n\npayment-service:\n  stripe_key:      \"sk_live_xxx\"\n  webhook_secret:  \"whsec_xxx\"\n  retry_attempts:  3\n\nnotification-service:\n  smtp_host: \"smtp.sendgrid.net\"\n  smtp_port: 587\n</code></pre> <p>Use a separate rule per service, each with its own <code>root_key</code>:</p> <pre><code>rules: [\n  {\n    platform:             \"github\"\n    datasource:           \"mongo\"\n    file_name:            \"configs/services.yaml\"\n    root_key:             \"auth-service\"\n    destination_template: \"{env}-auth\"\n    repositories:         [\"my-org/monorepo\"]\n    branch_map: { main: \"production\", develop: \"staging\" }\n  },\n  {\n    platform:             \"github\"\n    datasource:           \"mongo\"\n    file_name:            \"configs/services.yaml\"\n    root_key:             \"payment-service\"\n    destination_template: \"{env}-payment\"\n    repositories:         [\"my-org/monorepo\"]\n    branch_map: { main: \"production\", develop: \"staging\" }\n  },\n]\n</code></pre> <p>The <code>auth-service</code> rule writes <code>jwt_secret</code>, <code>token_ttl</code>, <code>refresh_enabled</code> to <code>production-auth</code>. The <code>payment-service</code> rule writes <code>stripe_key</code>, <code>webhook_secret</code>, <code>retry_attempts</code> to <code>production-payment</code>. Neither rule sees the other service's keys.</p>"},{"location":"examples/#helm-valuesyaml-extract-the-app-block","title":"Helm <code>values.yaml</code> \u2014 extract the app block","text":"<p>A <code>values.yaml</code> mixes Kubernetes deployment config with application config. Only the <code>app</code> block should be pushed to Redis:</p> <pre><code>replicaCount: 3\nimage:\n  repository: my-org/my-service\n  tag: \"1.4.2\"\nresources:\n  limits:\n    cpu: \"500m\"\n    memory: \"256Mi\"\n\napp:\n  log_level:       warn\n  max_connections: 200\n  cache_ttl:       300\n  feature_flags:\n    dark_mode:    true\n    new_checkout: false\n</code></pre> <pre><code>rules: [{\n  file_name: \"helm/values.yaml\"\n  root_key:  \"app\"\n  datasource: \"redis\"\n  destination_template: \"{env}:cfg\"\n}]\n</code></pre> <p>Keys written to Redis: <code>log_level</code>, <code>max_connections</code>, <code>cache_ttl</code>, <code>feature_flags.dark_mode</code>, <code>feature_flags.new_checkout</code>. The Kubernetes fields (<code>replicaCount</code>, <code>image</code>, <code>resources</code>) are never touched.</p>"},{"location":"examples/#multiple-files","title":"Multiple files","text":"<p>Each rule targets one file. To sync multiple files to the same (or different) sinks, add more rules:</p> <pre><code>rules: [\n  {\n    platform:             \"github\"\n    datasource:           \"mongo\"\n    file_name:            \"configs/app.yaml\"\n    repositories:         [\"my-org/*\"]\n    destination_template: \"{env}-app\"\n  },\n  {\n    platform:             \"github\"\n    datasource:           \"mongo\"\n    file_name:            \"configs/database.yaml\"\n    repositories:         [\"my-org/*\"]\n    destination_template: \"{env}-db\"\n  },\n]\n</code></pre>"},{"location":"examples/#multi-sink-fan-out","title":"Multi-sink fan-out","text":"<p>One push writes to MongoDB, Redis, and ZooKeeper simultaneously.</p> <pre><code>bundle: {\n  platforms: [{\n    github: {\n      endpoint: \"https://github.com\"\n      secret:   \"my-webhook-secret\"\n      token:    \"ghp_xxx\"\n    }\n  }]\n\n  datasources: [\n    { mongo:     { endpoint: \"mongodb://mongo:27017\",  database: \"vartrack\" } },\n    { redis:     { host: \"redis\", port: 6379 } },\n    { zookeeper: { hosts: [\"zookeeper:2181\"] } },\n  ]\n\n  rules: [\n    {\n      platform:             \"github\"\n      datasource:           \"mongo\"\n      file_name:            \"configs/app.yaml\"\n      repositories:         [\"my-org/*\"]\n      destination_template: \"{env}-config\"\n      branch_map: { main: \"production\", develop: \"staging\" }\n    },\n    {\n      platform:             \"github\"\n      datasource:           \"redis\"\n      file_name:            \"configs/app.yaml\"\n      repositories:         [\"my-org/*\"]\n      destination_template: \"{env}:cfg\"\n      branch_map: { main: \"production\", develop: \"staging\" }\n    },\n    {\n      platform:             \"github\"\n      datasource:           \"zookeeper\"\n      file_name:            \"configs/app.yaml\"\n      repositories:         [\"my-org/*\"]\n      destination_template: \"/myapp/{env}\"\n      branch_map: { main: \"production\", develop: \"staging\" }\n    },\n  ]\n}\n</code></pre>"},{"location":"examples/#pr-preview-environments","title":"PR preview environments","text":"<p>Each pull request gets its own isolated collection in MongoDB.</p> <pre><code>rules: [{\n  platform:             \"github\"\n  datasource:           \"mongo\"\n  file_name:            \"configs/app.yaml\"\n  repositories:         [\"my-org/app\"]\n  destination_template: \"{env}-config\"\n  env_as_pr:            true    // PR #42 \u2192 collection \"pr-42-config\"\n  self_heal:            false   // preview envs don't need self-heal\n}]\n</code></pre>"},{"location":"examples/#s3-linux-server","title":"S3 + Linux server","text":"<p>Write config to S3 and also deploy a <code>.env</code> file to a Linux server.</p> <pre><code>datasources: [\n  {\n    s3: {\n      bucket:            \"my-configs\"\n      region:            \"us-east-1\"\n      access_key_id:     \"AKIA...\"\n      secret_access_key: \"...\"\n    }\n  },\n  {\n    linux_server: {\n      host:        \"server.example.com\"\n      username:    \"deploy\"\n      private_key: \"-----BEGIN OPENSSH PRIVATE KEY-----\\n...\"\n    }\n  }\n]\n\nrules: [\n  {\n    platform:             \"github\"\n    datasource:           \"s3\"\n    file_name:            \"configs/app.yaml\"\n    repositories:         [\"my-org/*\"]\n    destination_template: \"configs/{env}/\"\n    branch_map: { main: \"production\" }\n  },\n  {\n    platform:             \"github\"\n    datasource:           \"linux_server\"\n    file_name:            \"configs/app.yaml\"\n    repositories:         [\"my-org/*\"]\n    destination_template: \"/etc/app/{env}.env\"\n    branch_map: { main: \"production\" }\n  }\n]\n</code></pre>"},{"location":"examples/#secret-value-forms","title":"Secret value forms","text":"<p>Every credential field accepts one of three forms:</p> <pre><code>// 1. Plain string\ntoken: \"ghp_xxx\"\n\n// 2. Mounted file (Docker secret / K8s secret volume) \u2014 read at startup\ntoken: {file: \"/run/secrets/github-pat\"}\n\n// 3. Vault reference \u2014 fetched at ETL time\ntoken: {ref: {path: \"vartrack/github\", key: \"token\"}}\n</code></pre>"},{"location":"examples/#vault-secrets","title":"Vault secrets","text":"<p>Reference secrets from HashiCorp Vault instead of storing them in the bundle.</p> <pre><code>bundle: {\n  secret_managers: [{\n    vault: {\n      endpoint:    \"https://vault.mycompany.com\"\n      mount_point: \"secret\"\n      kv_version:  2\n      token_auth: {\n        token: {file: \"/run/secrets/vault-token\"}   // vault token from a mounted file\n      }\n    }\n  }]\n\n  platforms: [{\n    github: {\n      endpoint: \"https://github.com\"\n      secret:   {ref: {path: \"vartrack/github\", key: \"webhook_secret\"}}  // from Vault\n      token:    {ref: {path: \"vartrack/github\", key: \"token\"}}\n    }\n  }]\n\n  datasources: [{\n    mongo: {\n      endpoint: \"mongodb://mongo:27017\"\n      database: \"vartrack\"\n      username: \"vartrack\"\n      password: {ref: {path: \"vartrack/mongo\", key: \"password\"}}         // from Vault\n    }\n  }]\n}\n</code></pre>"},{"location":"examples/#github-enterprise","title":"GitHub Enterprise","text":"<p>Connect to a self-hosted GitHub Enterprise instance.</p> <pre><code>platforms: [{\n  github: {\n    endpoint:        \"https://git.mycompany.com\"   // GHE URL\n    protocol:        \"https\"\n    push_event_name: \"push\"\n    pr_event_name:   \"pull_request\"\n    secret:          \"my-webhook-secret\"\n    token:           \"ghp_xxx\"\n    verify_ssl:      true\n    org_name:        \"my-org\"\n  }\n}]\n</code></pre>"},{"location":"examples/#cue-schema-validation","title":"CUE schema validation","text":"<p>Validate every config push against a per-tenant CUE schema.</p> <pre><code>// In your schema repo: schemas/myapp.cue\n#Config: {\n  database_host:   string\n  max_connections: int &amp; &gt;=1 &amp; &lt;=1000\n  log_level:       \"debug\" | \"info\" | \"warn\" | \"error\"\n}\n</code></pre> <pre><code>// In config.cue\nschema_registry: {\n  platform: \"github\"\n  repo:     \"my-org/schemas\"\n  branch:   \"main\"\n}\n</code></pre> <p>varTrack fetches the schema, runs <code>cue vet</code>, and rejects the payload before any write if validation fails.</p>"},{"location":"examples/gitea/","title":"Gitea platform example","text":"<p>Gitea is a self-hosted Git service. varTrack supports it natively \u2014 the payload format is identical to GitHub and the ETL pipeline is shared; only the webhook headers differ.</p> Header Value <code>X-Gitea-Event</code> <code>push</code> / <code>pull_request</code> <code>X-Gitea-Signature</code> HMAC-SHA256 hex digest (no <code>sha256=</code> prefix) <code>X-Gitea-Delivery</code> Unique delivery UUID (replay protection)"},{"location":"examples/gitea/#full-bundle","title":"Full bundle","text":"<pre><code>bundle: {\n  platforms: [{\n    gitea: {\n      endpoint:    \"https://gitea.mycompany.com\"\n      protocol:    \"https\"\n      token:       \"gta_xxxxxxxxxxxxxxxxxxxxxxxxxxxx\"  // personal access token\n      secret:      \"my-webhook-secret\"                // HMAC signing secret\n      org_name:    \"my-org\"\n      verify_ssl:  true\n      timeout:     \"30s\"\n      max_retries: 3\n      page_size:   50\n    }\n  }]\n\n  datasources: [\n    { mongo: { endpoint: \"mongodb://mongo:27017\", database: \"vartrack\" } },\n    { redis: { tag: \"broker\",  hosts: [\"redis:6379\"], database: 0 } },\n    { redis: { tag: \"results\", hosts: [\"redis:6379\"], database: 1 } },\n  ]\n\n  rules: [{\n    platform:             \"gitea\"\n    datasource:           \"mongo\"\n    file_name:            \"configs/app.yaml\"\n    repositories:         [\"my-org/*\"]\n    destination_template: \"{env}-config\"\n    sync_mode:            \"AUTO\"\n    self_heal:            true\n    branch_map: {\n      main:    \"production\"\n      develop: \"staging\"\n    }\n    prune: {\n      last:    true\n      dry_run: false\n    }\n  }]\n\n  global_tags: {\n    celery_broker_datasource:  \"redis-broker\"\n    celery_backend_datasource: \"redis-results\"\n  }\n}\n</code></pre>"},{"location":"examples/gitea/#webhook-setup-in-gitea","title":"Webhook setup in Gitea","text":"<ol> <li>Open your repository \u2192 Settings \u2192 Webhooks \u2192 Add Webhook \u2192 Gitea</li> <li>Set Payload URL to <code>http://gateway:5657/webhooks/mongo</code></li> <li>Set Content type to <code>application/json</code></li> <li>Set Secret to the same value as <code>secret</code> in your bundle</li> <li>Under Trigger On, select Push Events and Pull Request Events</li> <li>Click Add Webhook</li> </ol> <p>Gitea will send a test ping \u2014 the gateway will reject it gracefully (no <code>X-Gitea-Event: push</code> header) and return <code>400</code>. That's expected.</p>"},{"location":"examples/gitea/#using-vault-secrets-for-the-token","title":"Using Vault secrets for the token","text":"<p>Keep the Gitea token out of the bundle file by referencing it from Vault:</p> <pre><code>gitea: {\n  endpoint: \"https://gitea.mycompany.com\"\n  token:    {ref: {path: \"vartrack/gitea\", key: \"token\"}}           // resolved from Vault at startup\n  secret:   {ref: {path: \"vartrack/gitea\", key: \"webhook_secret\"}}\n}\n</code></pre> <p>Vault setup:</p> <pre><code>vault kv put secret/vartrack/gitea \\\n  token=gta_xxxxxxxxxxxxxxxxxxxxxxxxxxxx \\\n  webhook_secret=my-webhook-secret\n</code></pre>"},{"location":"examples/gitea/#differences-from-github","title":"Differences from GitHub","text":"GitHub Gitea Signature header <code>X-Hub-Signature-256: sha256=&lt;hex&gt;</code> <code>X-Gitea-Signature: &lt;hex&gt;</code> Signature format <code>sha256=</code> prefix + hex raw hex (no prefix) Delivery header <code>X-GitHub-Delivery</code> <code>X-Gitea-Delivery</code> API base path <code>/api/v3</code> (GHE) or <code>api.github.com</code> <code>/api/v1</code> Token auth header <code>Authorization: Bearer &lt;token&gt;</code> <code>Authorization: token &lt;token&gt;</code> Payload structure GitHub format GitHub-compatible (same fields)"},{"location":"services/gateway/","title":"gateway-service","text":"<p>The gateway-service is the HTTP ingress layer of varTrack. It receives webhooks from Git platforms, routes them to the correct datasource rule, and forwards them to the orchestrator-service via gRPC.</p>"},{"location":"services/gateway/#what-it-does","title":"What it does","text":"<pre><code>GitHub webhook  \u2500\u2500HTTPS\u2500\u2500\u25ba  gateway-service  \u2500\u2500gRPC\u2500\u2500\u25ba  orchestrator-service\n                             :5657                         :50051\n                                \u2502\n                             :9090  metrics + health\n</code></pre> <ol> <li>Receives a <code>push</code> or <code>pull_request</code> event</li> <li>Matches the request to a datasource rule in the bundle</li> <li>Forwards the payload to the orchestrator via gRPC</li> <li>Returns <code>202 Accepted</code> \u2014 processing is async</li> </ol>"},{"location":"services/gateway/#running","title":"Running","text":"<pre><code>APP_ENV=dev \\\nORCHESTRATOR_ADDR=localhost:50051 \\\nCONFIG_PATH=config.cue \\\ngo run ./gateway-service/cmd\n</code></pre>"},{"location":"services/gateway/#with-docker","title":"With Docker","text":"<pre><code>docker run \\\n  -p 5657:5657 \\\n  -p 9090:9090 \\\n  -v ./config.cue:/config.cue:ro \\\n  -e APP_ENV=production \\\n  -e ORCHESTRATOR_ADDR=orchestrator:50051 \\\n  -e CONFIG_PATH=/config.cue \\\n  jarin-devoss/gateway-service:latest\n</code></pre>"},{"location":"services/gateway/#environment-variables","title":"Environment variables","text":"Variable Default Description <code>APP_ENV</code> <code>test</code> <code>production</code> enables TLS and signature enforcement <code>LOG_LEVEL</code> <code>INFO</code> <code>DEBUG</code>, <code>INFO</code>, <code>WARN</code>, <code>ERROR</code> <code>GATEWAY_ADDR</code> <code>:5657</code> Webhook listener <code>ORCHESTRATOR_ADDR</code> <code>localhost:50051</code> gRPC address of the orchestrator <code>CONFIG_PATH</code> <code>config.cue</code> CUE bundle path <code>ADMIN_ADDR</code> <code>:9090</code> Metrics + health listener <code>GRPC_TLS_CERT</code> \u2014 Client cert for mTLS outbound gRPC <code>GRPC_TLS_KEY</code> \u2014 Client key for mTLS outbound gRPC <code>GATEWAY_TLS_CERT</code> \u2014 Inbound TLS cert <code>GATEWAY_TLS_KEY</code> \u2014 Inbound TLS key"},{"location":"services/gateway/#api","title":"API","text":""},{"location":"services/gateway/#webhook-endpoint","title":"Webhook endpoint","text":"<pre><code>POST /webhooks/{datasource}\n</code></pre> Status Description <code>202 Accepted</code> Webhook forwarded to orchestrator <code>200 OK</code> Event type not handled (e.g. <code>ping</code>) <code>400</code> Bad request (missing header, invalid JSON, validation error) <code>401</code> Signature invalid or replay detected <code>404</code> No rule configured for this datasource <code>413</code> Request body exceeds <code>max_webhook_body_bytes</code> <code>429</code> Rate limit exceeded <code>502</code> gRPC call to orchestrator failed <code>503</code> Circuit breaker open"},{"location":"services/gateway/#health","title":"Health","text":"<pre><code>GET /health/liveness     # 200 while alive\nGET /health/readiness    # 200 when orchestrator connection is ready\nGET /metrics             # Prometheus metrics (port 9090)\n</code></pre>"},{"location":"services/orchestrator/","title":"orchestrator-service","text":"<p>The orchestrator-service runs the ETL pipeline. It receives webhook events from the gateway via gRPC, fetches the changed file from Git, parses and validates it, then writes the config values to every configured sink.</p>"},{"location":"services/orchestrator/#what-it-does","title":"What it does","text":"<pre><code>gateway (gRPC) \u2192 Celery task \u2192 fetch Git \u2192 parse \u2192 flatten \u2192 validate CUE \u2192 write sinks\n</code></pre> <p>Three stages:</p> <ol> <li>Payload \u2014 resolve rule, platform, and target environment</li> <li>ETL \u2014 fetch from Git, parse, flatten to <code>key: value</code> map, validate against CUE schema</li> <li>Sync \u2014 write to sinks, prune stale keys, rollback on failure</li> </ol>"},{"location":"services/orchestrator/#running","title":"Running","text":"<pre><code>cd orchestrator-service\npip install -e .\n\n# API + gRPC server\nuvicorn main:app --host 0.0.0.0 --port 8000\n\n# Celery worker (separate terminal)\ncelery -A app.worker.celery worker --queues=webhooks,sync -l info\n</code></pre>"},{"location":"services/orchestrator/#with-docker","title":"With Docker","text":"<pre><code># API server\ndocker run \\\n  -p 8000:8000 \\\n  -p 50051:50051 \\\n  -e CONFIG_PATH=/config.cue \\\n  -v ./config.cue:/config.cue:ro \\\n  jarin-devoss/orchestrator-service:latest\n\n# Celery worker\ndocker run \\\n  -e CONFIG_PATH=/config.cue \\\n  -v ./config.cue:/config.cue:ro \\\n  jarin-devoss/orchestrator-service:latest \\\n  celery -A app.worker.celery worker --queues=webhooks,sync -l info\n</code></pre>"},{"location":"services/orchestrator/#environment-variables","title":"Environment variables","text":"Variable Default Description <code>APP_ENV</code> <code>development</code> <code>production</code> enforces mTLS <code>CELERY_BROKER_URL</code> <code>redis://localhost:6379/0</code> Celery broker <code>CONFIG_PATH</code> <code>config.cue</code> CUE bundle path <code>GIT_CACHE_DIR</code> <code>/tmp/vt_gitcache</code> Git repo LRU cache directory <code>GIT_CACHE_MAX_ENTRIES</code> <code>20</code> Max cached repo checkouts <code>SCHEMA_CACHE_DIR</code> <code>/tmp/schema_registry</code> CUE schema clone directory <code>ORCHESTRATOR_ADDR</code> <code>:50051</code> gRPC listen address"},{"location":"services/orchestrator/#api","title":"API","text":""},{"location":"services/orchestrator/#http-port-8000","title":"HTTP (port 8000)","text":"Method Path Description <code>POST</code> <code>/v1/webhooks/{datasource}</code> Ingest webhook, enqueue ETL task <code>POST</code> <code>/v1/webhooks/{datasource}/dry-run</code> Simulate without writing <code>GET</code> <code>/v1/tasks/{task_id}</code> Poll task status <code>GET</code> <code>/v1/health</code> Liveness probe <code>GET</code> <code>/metrics</code> Prometheus metrics <code>GET</code> <code>/docs</code> OpenAPI / Swagger UI"},{"location":"services/orchestrator/#grpc-port-50051","title":"gRPC (port 50051)","text":"RPC Called by Description <code>ProcessWebhook</code> gateway-service Enqueue ETL pipeline <code>TriggerSync</code> watcher-service Re-sync datasource on drift"},{"location":"services/watcher/","title":"watcher-service","text":"<p>The watcher-service continuously polls your configured datasources and compares the live state against the Git baseline. When drift is detected and <code>self_heal: true</code> is set, it calls the orchestrator to restore correct state automatically.</p>"},{"location":"services/watcher/#what-it-does","title":"What it does","text":"<pre><code>Every POLL_INTERVAL\n  \u251c\u2500\u2500 Read live values from each datasource\n  \u251c\u2500\u2500 Compare against Git baseline\n  \u251c\u2500\u2500 No diff   \u2192 OK\n  \u2514\u2500\u2500 Drift     \u2192 log + increment metric + (self_heal? \u2192 TriggerSync)\n</code></pre>"},{"location":"services/watcher/#running","title":"Running","text":"<pre><code>ORCHESTRATOR_ADDR=localhost:50051 \\\nCONFIG_PATH=config.cue \\\nPOLL_INTERVAL=60s \\\ngo run ./watcher-service/cmd\n</code></pre>"},{"location":"services/watcher/#with-docker","title":"With Docker","text":"<pre><code>docker run \\\n  -p 9091:9091 \\\n  -v ./config.cue:/app/config.cue:ro \\\n  -e APP_ENV=production \\\n  -e ORCHESTRATOR_ADDR=orchestrator:50051 \\\n  -e CONFIG_PATH=/app/config.cue \\\n  -e POLL_INTERVAL=60s \\\n  jarin-devoss/watcher-service:latest\n</code></pre>"},{"location":"services/watcher/#environment-variables","title":"Environment variables","text":"Variable Default Description <code>APP_ENV</code> <code>development</code> <code>production</code> requires TLS credentials <code>CONFIG_PATH</code> <code>config.cue</code> CUE bundle path <code>ORCHESTRATOR_ADDR</code> <code>localhost:50051</code> gRPC address of the orchestrator <code>POLL_INTERVAL</code> <code>60s</code> How often each sink is polled <code>HEAL_TIMEOUT</code> <code>30s</code> Max time for a self-heal call <code>ADMIN_ADDR</code> <code>:9091</code> Health + metrics listener <code>OTEL_ENABLED</code> <code>false</code> Enable OpenTelemetry tracing <code>ELK_ENABLED</code> <code>false</code> Enable Elasticsearch log shipping"},{"location":"services/watcher/#health-metrics","title":"Health + metrics","text":"<pre><code>GET /health/liveness    # 200 while alive\nGET /health/readiness   # 200 when orchestrator connection is ready\nGET /metrics            # Prometheus metrics (port 9091)\n</code></pre> <p>Key metrics:</p> Metric Description <code>vartrack_watcher_drift_total</code> Drift events by datasource <code>vartrack_watcher_heal_total</code> Self-heal calls triggered <code>vartrack_watcher_poll_duration_seconds</code> Poll cycle duration"},{"location":"sinks/","title":"Sinks","text":"<p>A sink is a datasource that varTrack writes config values into. One push can fan out to multiple sinks simultaneously \u2014 just add multiple rules pointing at different datasources.</p>"},{"location":"sinks/#available-sinks","title":"Available sinks","text":"Sink Use case MongoDB Feature flags, app config, tenant settings stored as documents Redis Low-latency config reads, real-time feature flags ZooKeeper Distributed coordination config, service discovery values S3 Config files as objects, audit storage, batch pipelines Kubernetes ConfigMap App config for Kubernetes workloads Helm Helm values files, chart config driven by Git Vercel Frontend environment variables synced from Git Linux Server Write config files directly to remote servers via SSH"},{"location":"sinks/#multi-sink-fan-out","title":"Multi-sink fan-out","text":"<p>Push once, write to all of them:</p> <pre><code>rules: [\n  { platform: \"github\", datasource: \"mongo\",      file_name: \"config.yaml\", destination_template: \"{env}-config\" },\n  { platform: \"github\", datasource: \"redis\",      file_name: \"config.yaml\", destination_template: \"{env}:cfg\" },\n  { platform: \"github\", datasource: \"zookeeper\",  file_name: \"config.yaml\", destination_template: \"/{tenant}/{env}\" },\n  { platform: \"github\", datasource: \"s3\",         file_name: \"config.yaml\", destination_template: \"{tenant}/{env}/\" },\n]\n</code></pre> <p>All four writes run in parallel on each push.</p>"},{"location":"sinks/#multiple-instances-of-the-same-type","title":"Multiple instances of the same type","text":"<p>Use <code>tag</code> to run two MongoDB instances side by side:</p> <pre><code>datasources: [\n  { mongo: { tag: \"primary\",  endpoint: \"mongodb://mongo-primary:27017\",  database: \"app\" } },\n  { mongo: { tag: \"replica\",  endpoint: \"mongodb://mongo-replica:27017\",  database: \"app\" } },\n]\n\nrules: [\n  { platform: \"github\", datasource: \"mongo-primary\", ... },\n  { platform: \"github\", datasource: \"mongo-replica\",  ... },\n]\n</code></pre> <p>The resolved datasource name is <code>mongo-{tag}</code> when a tag is set, or <code>mongo</code> when the tag is empty.</p>"},{"location":"sinks/#destination-template","title":"Destination template","text":"<p>Every sink supports <code>destination_template</code> to control where data lands using <code>{env}</code> and <code>{tenant}</code> placeholders. See the Destination Template page for the full reference.</p>"},{"location":"sinks/configmap/","title":"Kubernetes ConfigMap Sink","text":"<p>varTrack writes config values directly into a Kubernetes ConfigMap. Changes pushed to Git are reflected in the cluster automatically \u2014 no manual <code>kubectl apply</code> needed.</p>"},{"location":"sinks/configmap/#configuration","title":"Configuration","text":"<pre><code>datasources: [{\n  configmap: {\n    tag:        \"\"\n    namespace:  \"default\"\n    kubeconfig: \"/path/to/kubeconfig\"    // optional \u2014 uses in-cluster config if omitted\n  }\n}]\n</code></pre>"},{"location":"sinks/configmap/#destination-template","title":"Destination template","text":"<p>The <code>destination_template</code> controls the ConfigMap name:</p> <pre><code>rules: [{\n  platform:             \"github\"\n  datasource:           \"configmap\"\n  destination_template: \"myapp-{env}\"\n}]\n</code></pre> Push branch ConfigMap created/updated <code>main</code> <code>myapp-production</code> <code>develop</code> <code>myapp-staging</code> <p>Config keys are stored as ConfigMap data entries:</p> <pre><code>apiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: myapp-production\n  namespace: default\ndata:\n  database.host: \"mongo.prod.internal\"\n  max_connections: \"50\"\n  feature.dark_mode: \"true\"\n</code></pre>"},{"location":"sinks/configmap/#drift-detection","title":"Drift detection","text":"<p>The watcher reads the ConfigMap data and compares against the Git baseline. Any changed or missing key triggers a drift event.</p> <pre><code>rules: [{\n  platform:   \"github\"\n  datasource: \"configmap\"\n  self_heal:  true\n}]\n</code></pre>"},{"location":"sinks/helm/","title":"Helm Sink","text":"<p>varTrack writes config values to a Helm release. Git becomes the source of truth for Helm chart configuration.</p>"},{"location":"sinks/helm/#configuration","title":"Configuration","text":"<pre><code>datasources: [{\n  helm: {\n    tag:        \"\"\n    namespace:  \"default\"\n    kubeconfig: \"/path/to/kubeconfig\"   // optional \u2014 uses in-cluster config if absent\n  }\n}]\n</code></pre>"},{"location":"sinks/helm/#destination-template","title":"Destination template","text":"<p>The <code>destination_template</code> sets the Helm release name:</p> <pre><code>rules: [{\n  platform:             \"github\"\n  datasource:           \"helm\"\n  destination_template: \"myapp-{env}\"\n}]\n</code></pre> Push branch Helm release updated <code>main</code> <code>myapp-production</code> <code>develop</code> <code>myapp-staging</code>"},{"location":"sinks/helm/#upgrade-strategies","title":"Upgrade strategies","text":"<pre><code>datasources: [{\n  helm: {\n    upgrade_strategy: \"VALUES_ONLY\"   // default\n  }\n}]\n</code></pre> Strategy What it does <code>VALUES_ONLY</code> Inject new values into the existing release; chart version unchanged <code>FULL_UPGRADE</code> Run a full <code>helm upgrade</code> \u2014 chart + values <code>INSTALL_OR_UPGRADE</code> Install the release if it doesn't exist, upgrade otherwise"},{"location":"sinks/helm/#values_only-default","title":"<code>VALUES_ONLY</code> (default)","text":"<p>Writes a <code>values.yaml</code> and applies it without changing the chart:</p> <pre><code># Generated values.yaml\ndatabase:\n  host: mongo.prod.internal\nmaxConnections: 50\nfeatureFlags:\n  darkMode: true\n</code></pre>"},{"location":"sinks/helm/#full_upgrade","title":"<code>FULL_UPGRADE</code>","text":"<p>Runs a full Helm upgrade with the new values:</p> <pre><code>helm upgrade myapp-production ./chart \\\n  --set database.host=mongo.prod.internal \\\n  --set maxConnections=50\n</code></pre>"},{"location":"sinks/helm/#install_or_upgrade","title":"<code>INSTALL_OR_UPGRADE</code>","text":"<p>Equivalent to <code>helm upgrade --install</code> \u2014 safe for first-time deployments:</p> <pre><code>helm upgrade --install myapp-production ./chart \\\n  -f generated-values.yaml\n</code></pre>"},{"location":"sinks/helm/#chart-configuration","title":"Chart configuration","text":"<pre><code>datasources: [{\n  helm: {\n    chart_name:    \"myapp\"\n    chart_version: \"1.2.3\"     // optional \u2014 pin chart version\n    chart_repo:    \"https://charts.myorg.com\"\n  }\n}]\n</code></pre>"},{"location":"sinks/helm/#upgrade-options","title":"Upgrade options","text":"<pre><code>datasources: [{\n  helm: {\n    wait:            true    // wait for all resources to be ready\n    wait_timeout:    \"5m\"   // timeout for --wait\n    atomic:          true    // roll back on failure\n    force:           false   // force resource updates\n    cleanup_on_fail: true    // delete new resources if upgrade fails\n    no_hooks:        false   // skip pre/post-upgrade hooks\n    max_history:     10      // number of release versions to keep\n  }\n}]\n</code></pre>"},{"location":"sinks/helm/#drift-detection","title":"Drift detection","text":"<p>The watcher reads the current Helm release values and compares against the Git baseline.</p> <pre><code>rules: [{\n  platform:   \"github\"\n  datasource: \"helm\"\n  self_heal:  true\n}]\n</code></pre>"},{"location":"sinks/linux-server/","title":"Linux Server Sink","text":"<p>varTrack writes config values as a file on a remote Linux server over SSH. Useful for servers that read configuration from flat files (<code>.env</code>, INI, etc.) and can't use a central datastore.</p>"},{"location":"sinks/linux-server/#configuration","title":"Configuration","text":"<pre><code>datasources: [{\n  linux_server: {\n    tag:         \"\"\n    host:        \"server.example.com\"\n    port:        22\n    username:    \"deploy\"\n    private_key: \"-----BEGIN OPENSSH PRIVATE KEY-----\\n...\"\n    base_path:   \"/etc/app\"\n  }\n}]\n</code></pre>"},{"location":"sinks/linux-server/#password-authentication","title":"Password authentication","text":"<pre><code>datasources: [{\n  linux_server: {\n    host:     \"server.example.com\"\n    username: \"deploy\"\n    password: \"secret\"\n  }\n}]\n</code></pre>"},{"location":"sinks/linux-server/#destination-template","title":"Destination template","text":"<p>The <code>destination_template</code> sets the full remote file path:</p> <pre><code>rules: [{\n  platform:             \"github\"\n  datasource:           \"linux_server\"\n  destination_template: \"/etc/app/{env}.env\"\n}]\n</code></pre> Push branch File written <code>main</code> <code>/etc/app/production.env</code> <code>develop</code> <code>/etc/app/staging.env</code> <p>The file is written in <code>KEY=value</code> format by default:</p> <pre><code># /etc/app/production.env\nDATABASE_HOST=mongo.prod.internal\nMAX_CONNECTIONS=50\nFEATURE_DARK_MODE=true\n</code></pre>"},{"location":"sinks/linux-server/#file-permissions-ownership","title":"File permissions &amp; ownership","text":"<pre><code>datasources: [{\n  linux_server: {\n    host:              \"server.example.com\"\n    username:          \"deploy\"\n    private_key:       \"...\"\n    file_permissions:  \"0640\"       // octal \u2014 owner rw, group r\n    file_owner:        \"app\"        // chown user\n    file_group:        \"appgroup\"   // chown group\n    create_parent_dirs: true        // mkdir -p before writing\n  }\n}]\n</code></pre>"},{"location":"sinks/linux-server/#backup-before-write","title":"Backup before write","text":"<pre><code>datasources: [{\n  linux_server: {\n    host:          \"server.example.com\"\n    username:      \"deploy\"\n    private_key:   \"...\"\n    backup_suffix: \".bak\"   // creates /etc/app/production.env.bak before overwrite\n  }\n}]\n</code></pre>"},{"location":"sinks/linux-server/#post-write-commands","title":"Post-write commands","text":"<p>Run shell commands on the server after the file is written (e.g. reload a service):</p> <pre><code>datasources: [{\n  linux_server: {\n    host:        \"server.example.com\"\n    username:    \"deploy\"\n    private_key: \"...\"\n    post_write_commands: [\n      \"systemctl reload nginx\",\n      \"systemctl restart myapp\",\n    ]\n    continue_on_command_error: false   // abort remaining commands on first failure\n  }\n}]\n</code></pre> <p>Commands run in order. Set <code>continue_on_command_error: true</code> to keep running even if one fails.</p>"},{"location":"sinks/linux-server/#sudo","title":"Sudo","text":"<pre><code>datasources: [{\n  linux_server: {\n    host:          \"server.example.com\"\n    username:      \"deploy\"\n    private_key:   \"...\"\n    use_sudo:      true\n    sudo_password: \"sudo-secret\"            // optional \u2014 for passworded sudo\n  }\n}]\n</code></pre>"},{"location":"sinks/linux-server/#sftp-mode","title":"SFTP mode","text":"<p>By default varTrack uses SCP. Switch to SFTP for environments where SCP is disabled:</p> <pre><code>datasources: [{\n  linux_server: {\n    host:        \"server.example.com\"\n    username:    \"deploy\"\n    private_key: \"...\"\n    enable_sftp: true\n  }\n}]\n</code></pre>"},{"location":"sinks/linux-server/#host-key-verification","title":"Host key verification","text":"<pre><code>datasources: [{\n  linux_server: {\n    host:                    \"server.example.com\"\n    username:                \"deploy\"\n    private_key:             \"...\"\n    host_key_verification:   \"strict\"           // \"strict\" (default), \"accept-new\", \"off\"\n    known_hosts_file:        \"~/.ssh/known_hosts\"\n  }\n}]\n</code></pre> Mode Behaviour <code>strict</code> Reject unknown hosts \u2014 recommended for production <code>accept-new</code> Accept new hosts on first connect, reject changed keys <code>off</code> Disable host key checking \u2014 not recommended"},{"location":"sinks/linux-server/#drift-detection","title":"Drift detection","text":"<p>The watcher connects via SSH, reads the remote file, and compares its contents against the Git baseline.</p> <pre><code>rules: [{\n  platform:   \"github\"\n  datasource: \"linux_server\"\n  self_heal:  true\n}]\n</code></pre>"},{"location":"sinks/mongodb/","title":"MongoDB Sink","text":"<p>varTrack writes config values to MongoDB as key-value documents. Three write strategies are supported: key-value (one document per key), document (one document containing all keys), and file (one document per file containing all keys as a nested map).</p>"},{"location":"sinks/mongodb/#configuration","title":"Configuration","text":"<pre><code>datasources: [{\n  mongo: {\n    tag:             \"\"                         // optional \u2014 resolves to \"mongo-{tag}\"\n    endpoint:        \"mongodb://mongo:27017\"\n    database:        \"vartrack\"\n    collection:      \"variables\"\n    update_strategy: \"STRATEGY_KEY_VALUE\"       // or \"STRATEGY_DOCUMENT\"\n  }\n}]\n</code></pre>"},{"location":"sinks/mongodb/#with-authentication","title":"With authentication","text":"<pre><code>datasources: [{\n  mongo: {\n    endpoint:  \"mongodb://mongo:27017\"\n    database:  \"vartrack\"\n    username:  \"vartrack\"\n    password:  \"secret\"                        // or a Vault reference\n  }\n}]\n</code></pre>"},{"location":"sinks/mongodb/#with-tls","title":"With TLS","text":"<pre><code>datasources: [{\n  mongo: {\n    endpoint:    \"mongodb://mongo:27017\"\n    database:    \"vartrack\"\n    tls_enabled: true\n    tls_ca_file: \"/etc/ssl/mongo-ca.pem\"\n  }\n}]\n</code></pre>"},{"location":"sinks/mongodb/#write-strategies","title":"Write strategies","text":""},{"location":"sinks/mongodb/#strategy_key_value-default","title":"<code>STRATEGY_KEY_VALUE</code> (default)","text":"<p>Each config key is stored as a separate document:</p> <pre><code>{ \"key\": \"database.host\",     \"value\": \"mongo.prod.internal\" }\n{ \"key\": \"max_connections\",   \"value\": \"50\" }\n{ \"key\": \"feature.dark_mode\", \"value\": \"true\" }\n</code></pre> <p>Reading a value: <code>db.collection.findOne({ key: \"database.host\" }).value</code></p>"},{"location":"sinks/mongodb/#strategy_document","title":"<code>STRATEGY_DOCUMENT</code>","text":"<p>All config keys are stored in a single document:</p> <pre><code>{\n  \"database.host\":     \"mongo.prod.internal\",\n  \"max_connections\":   \"50\",\n  \"feature.dark_mode\": \"true\"\n}\n</code></pre> <p>Reading a value: <code>db.collection.findOne({})[\"database.host\"]</code></p>"},{"location":"sinks/mongodb/#strategy_file","title":"<code>STRATEGY_FILE</code>","text":"<p>The entire key set is stored as a single document with a nested <code>data</code> map, one document per synced file:</p> <pre><code>{\n  \"_vt_env\":       \"production\",\n  \"_vt_file_path\": \"configs/app.yaml\",\n  \"data\": {\n    \"database.host\":   \"mongo.prod.internal\",\n    \"max_connections\": \"50\"\n  }\n}\n</code></pre> <p>Efficient for large key sets since it only touches one document per sync.</p>"},{"location":"sinks/mongodb/#per-environment-collections","title":"Per-environment collections","text":"<p>Set <code>env_as_collection: true</code> to write each environment into its own collection instead of a fixed one:</p> <pre><code>datasources: [{\n  mongo: {\n    database:          \"vartrack\"\n    collection:        \"configs\"\n    env_as_collection: true   // production \u2192 \"configs_production\", pr-42 \u2192 \"configs_pr-42\"\n  }\n}]\n</code></pre>"},{"location":"sinks/mongodb/#destination-template","title":"Destination template","text":"<p>The <code>destination_template</code> field controls which collection data is written to:</p> <pre><code>rules: [{\n  platform:             \"github\"\n  datasource:           \"mongo\"\n  destination_template: \"{env}-config\"\n  branch_map: {\n    main:    \"production\"\n    develop: \"staging\"\n  }\n}]\n</code></pre> Push branch Collection written <code>main</code> <code>production-config</code> <code>develop</code> <code>staging-config</code> PR #42 <code>pr-42-config</code>"},{"location":"sinks/mongodb/#drift-detection","title":"Drift detection","text":"<p>The watcher-service reads all documents from the configured collection and compares <code>key \u2192 value</code> pairs against the Git baseline. Any added, changed, or deleted key triggers a drift event.</p> <p>Enable self-heal to restore automatically:</p> <pre><code>rules: [{\n  platform:   \"github\"\n  datasource: \"mongo\"\n  self_heal:  true\n}]\n</code></pre>"},{"location":"sinks/redis/","title":"Redis Sink","text":"<p>varTrack writes config values to Redis. Three data structures are supported: Hash (fields under one key), String (one key per variable), and JSON (Redis Stack).</p>"},{"location":"sinks/redis/#configuration","title":"Configuration","text":"<pre><code>datasources: [{\n  redis: {\n    tag:      \"\"                 // optional \u2014 resolves to \"redis-{tag}\"\n    host:     \"redis\"\n    port:     6379\n    password: \"secret\" // optional\n    db:       0\n  }\n}]\n</code></pre>"},{"location":"sinks/redis/#deployment-modes","title":"Deployment modes","text":"<pre><code>datasources: [{\n  redis: {\n    deployment_mode: \"STANDALONE\"   // default\n    host:            \"redis\"\n    port:            6379\n  }\n}]\n</code></pre> Mode When to use <code>STANDALONE</code> Single Redis instance (default) <code>CLUSTER</code> Redis Cluster \u2014 provide multiple hosts <code>SENTINEL</code> Redis Sentinel HA \u2014 provide sentinel addresses"},{"location":"sinks/redis/#cluster","title":"Cluster","text":"<pre><code>datasources: [{\n  redis: {\n    deployment_mode: \"CLUSTER\"\n    hosts: [\"redis-node-1:6379\", \"redis-node-2:6379\", \"redis-node-3:6379\"]\n  }\n}]\n</code></pre>"},{"location":"sinks/redis/#sentinel","title":"Sentinel","text":"<pre><code>datasources: [{\n  redis: {\n    deployment_mode: \"SENTINEL\"\n    hosts:        [\"sentinel-1:26379\", \"sentinel-2:26379\"]\n    sentinel_master: \"mymaster\"\n    password:     \"secret\"\n  }\n}]\n</code></pre>"},{"location":"sinks/redis/#data-structures","title":"Data structures","text":"<pre><code>datasources: [{\n  redis: {\n    data_structure: \"HASH\"    // default \u2014 all fields under one hash key\n    // data_structure: \"STRING\"  // one key per variable\n    // data_structure: \"JSON\"    // Redis Stack JSON (requires RedisJSON module)\n  }\n}]\n</code></pre>"},{"location":"sinks/redis/#hash-default","title":"<code>HASH</code> (default)","text":"<p>All config keys are stored as fields of a single hash. The hash key is the <code>destination_template</code>.</p> <pre><code>HSET production:cfg database.host \"mongo.prod.internal\"\nHSET production:cfg max_connections \"50\"\nHSET production:cfg feature.dark_mode \"true\"\n</code></pre> <p>Reading: <code>redis-cli HGET production:cfg database.host</code></p>"},{"location":"sinks/redis/#string","title":"<code>STRING</code>","text":"<p>Each config key is a separate Redis key, prefixed by <code>destination_template</code>.</p> <pre><code>SET production:cfg:database.host \"mongo.prod.internal\"\nSET production:cfg:max_connections \"50\"\n</code></pre> <p>Reading: <code>redis-cli GET production:cfg:database.host</code></p>"},{"location":"sinks/redis/#json","title":"<code>JSON</code>","text":"<p>Stores the entire config as a JSON document using RedisJSON. Requires the RedisJSON module.</p> <pre><code>JSON.SET production:cfg $ '{\"database\":{\"host\":\"mongo.prod.internal\"},\"max_connections\":\"50\"}'\n</code></pre> <p>Reading: <code>redis-cli JSON.GET production:cfg $.database.host</code></p>"},{"location":"sinks/redis/#destination-template","title":"Destination template","text":"<p>The <code>destination_template</code> sets the key prefix or hash name:</p> <pre><code>rules: [{\n  platform:             \"github\"\n  datasource:           \"redis\"\n  destination_template: \"{env}:cfg\"\n}]\n</code></pre>"},{"location":"sinks/redis/#tls","title":"TLS","text":"<pre><code>datasources: [{\n  redis: {\n    host:       \"redis\"\n    port:       6380\n    enable_tls: true\n    tls_ca:     \"/etc/ssl/redis-ca.pem\"       // optional CA cert\n    tls_cert:   \"/etc/ssl/redis-client.pem\"   // optional mTLS cert\n    tls_key:    \"/etc/ssl/redis-client.key\"   // optional mTLS key\n  }\n}]\n</code></pre>"},{"location":"sinks/redis/#drift-detection","title":"Drift detection","text":"<p>The watcher reads all keys/fields from the configured prefix and compares against the Git baseline. Any changed or missing field triggers a drift event.</p> <pre><code>rules: [{\n  platform:   \"github\"\n  datasource: \"redis\"\n  self_heal:  true\n}]\n</code></pre>"},{"location":"sinks/s3/","title":"S3 Sink","text":"<p>varTrack writes config values to S3 as objects. S3 is ideal for audit storage, batch pipelines, and config files read by many services.</p>"},{"location":"sinks/s3/#configuration","title":"Configuration","text":"<pre><code>datasources: [{\n  s3: {\n    tag:               \"\"\n    bucket:            \"my-config-bucket\"\n    region:            \"us-east-1\"\n    access_key_id:      \"AKIA...\"\n    secret_access_key:  \"...\"\n    endpoint:          \"\"    // optional \u2014 set for MinIO or other S3-compatible stores\n  }\n}]\n</code></pre>"},{"location":"sinks/s3/#minio-s3-compatible","title":"MinIO / S3-compatible","text":"<pre><code>datasources: [{\n  s3: {\n    bucket:            \"configs\"\n    endpoint:          \"http://minio:9000\"\n    access_key_id:      \"minioadmin\"\n    secret_access_key:  \"minioadmin\"\n    region:            \"us-east-1\"\n  }\n}]\n</code></pre>"},{"location":"sinks/s3/#iam-role-no-credentials","title":"IAM role (no credentials)","text":"<p>For services running on AWS with an instance role or IRSA:</p> <pre><code>datasources: [{\n  s3: {\n    bucket:       \"my-config-bucket\"\n    region:       \"us-east-1\"\n    use_iam_role: true   // no access_key_id / secret_access_key needed\n  }\n}]\n</code></pre>"},{"location":"sinks/s3/#destination-template","title":"Destination template","text":"<p>The <code>destination_template</code> sets the S3 key prefix:</p> <pre><code>rules: [{\n  platform:             \"github\"\n  datasource:           \"s3\"\n  destination_template: \"{tenant}/{env}/\"\n}]\n</code></pre> <p>Config keys are stored as objects under the prefix:</p> <pre><code>s3://my-config-bucket/acme/production/database.host\ns3://my-config-bucket/acme/production/max_connections\ns3://my-config-bucket/acme/production/feature.dark_mode\n</code></pre>"},{"location":"sinks/s3/#one-file-per-key-vs-one-object-per-key","title":"One file per key vs one object per key","text":"<p>By default each config key becomes a separate S3 object. Set <code>one_file_per_key: false</code> to write everything into a single JSON object:</p> <pre><code>datasources: [{\n  s3: {\n    bucket:          \"my-config-bucket\"\n    one_file_per_key: false   // write one JSON blob instead of per-key objects\n  }\n}]\n</code></pre>"},{"location":"sinks/s3/#server-side-encryption","title":"Server-side encryption","text":"<pre><code>datasources: [{\n  s3: {\n    bucket:               \"my-config-bucket\"\n    sse_algorithm:        \"AES256\"     // or \"aws:kms\"\n    // sse_kms_key_id:    \"arn:aws:kms:...\"  // only for aws:kms\n  }\n}]\n</code></pre> Algorithm Description <code>AES256</code> S3-managed keys (SSE-S3) <code>aws:kms</code> AWS KMS key (SSE-KMS) \u2014 specify <code>sse_kms_key_id</code>"},{"location":"sinks/s3/#object-versioning","title":"Object versioning","text":"<pre><code>datasources: [{\n  s3: {\n    bucket:            \"my-config-bucket\"\n    enable_versioning: true   // enable S3 bucket versioning\n  }\n}]\n</code></pre> <p>When enabled, each write creates a new object version, preserving full config history.</p>"},{"location":"sinks/s3/#drift-detection","title":"Drift detection","text":"<p>The watcher lists all objects under the configured prefix and compares their ETags against the baseline. Objects are only re-downloaded on ETag mismatch, keeping bandwidth low.</p> <pre><code>rules: [{\n  platform:   \"github\"\n  datasource: \"s3\"\n  self_heal:  true\n}]\n</code></pre>"},{"location":"sinks/vercel/","title":"Vercel Sink","text":"<p>varTrack writes config values to Vercel environment variables. Git becomes the source of truth for your Vercel project configuration.</p>"},{"location":"sinks/vercel/#configuration","title":"Configuration","text":"<pre><code>datasources: [{\n  vercel: {\n    tag:        \"\"\n    token:      \"vercel_token_xxx\"\n    project_id: \"prj_xxxx\"\n    team_id:    \"team_xxxx\"   // optional \u2014 for team-scoped projects\n  }\n}]\n</code></pre>"},{"location":"sinks/vercel/#destination-template","title":"Destination template","text":"<p>The <code>destination_template</code> sets the environment variable prefix:</p> <pre><code>rules: [{\n  platform:             \"github\"\n  datasource:           \"vercel\"\n  destination_template: \"{env}_\"\n}]\n</code></pre> Config key Vercel env var <code>DATABASE_HOST</code> <code>production_DATABASE_HOST</code> <code>MAX_CONNECTIONS</code> <code>production_MAX_CONNECTIONS</code>"},{"location":"sinks/vercel/#deployment-targets","title":"Deployment targets","text":"<p>Control which Vercel deployment environments receive the variables:</p> <pre><code>datasources: [{\n  vercel: {\n    token:      \"vercel_token_xxx\"\n    project_id: \"prj_xxxx\"\n    targets: [\"production\", \"preview\", \"development\"]  // all enabled by default\n  }\n}]\n</code></pre> Target When it applies <code>production</code> Production deployments (<code>main</code> branch by default) <code>preview</code> Preview deployments (feature branches / PRs) <code>development</code> Local <code>vercel dev</code> environment"},{"location":"sinks/vercel/#variable-type","title":"Variable type","text":"<pre><code>datasources: [{\n  vercel: {\n    token:      \"vercel_token_xxx\"\n    project_id: \"prj_xxxx\"\n    var_type:   \"plain\"     // \"plain\" (default), \"secret\", \"sensitive\"\n  }\n}]\n</code></pre> Type Description <code>plain</code> Readable in the Vercel dashboard <code>secret</code> Encrypted \u2014 not visible in the dashboard <code>sensitive</code> Same as secret; can't be read back via API after creation"},{"location":"sinks/vercel/#drift-detection","title":"Drift detection","text":"<p>The watcher reads Vercel environment variables and compares against the Git baseline.</p> <pre><code>rules: [{\n  platform:   \"github\"\n  datasource: \"vercel\"\n  self_heal:  true\n}]\n</code></pre>"},{"location":"sinks/zookeeper/","title":"ZooKeeper Sink","text":"<p>varTrack writes config values to ZooKeeper as znodes. ZooKeeper is ideal for distributed coordination config and service discovery.</p>"},{"location":"sinks/zookeeper/#configuration","title":"Configuration","text":"<pre><code>datasources: [{\n  zookeeper: {\n    tag:      \"\"\n    hosts:    [\"zookeeper:2181\"]\n    base_path: \"/vartrack\"       // optional root path\n  }\n}]\n</code></pre>"},{"location":"sinks/zookeeper/#with-authentication","title":"With authentication","text":"<pre><code>datasources: [{\n  zookeeper: {\n    hosts:         [\"zookeeper:2181\"]\n    auth_username: \"vartrack\"\n    auth_password: \"secret\"\n    auth_scheme:   \"digest\"    // default\n  }\n}]\n</code></pre>"},{"location":"sinks/zookeeper/#destination-template","title":"Destination template","text":"<p>The <code>destination_template</code> sets the root znode path under which config keys are written as child znodes:</p> <pre><code>rules: [{\n  platform:             \"github\"\n  datasource:           \"zookeeper\"\n  destination_template: \"/{tenant}/{env}\"\n}]\n</code></pre> <p>Each config key becomes a child znode:</p> <pre><code>/acme/production/database.host     = \"mongo.prod.internal\"\n/acme/production/max_connections   = \"50\"\n/acme/production/feature.dark_mode = \"true\"\n</code></pre>"},{"location":"sinks/zookeeper/#znode-encoding","title":"Znode encoding","text":"<pre><code>datasources: [{\n  zookeeper: {\n    hosts:    [\"zookeeper:2181\"]\n    encoding: \"plain\"    // \"plain\" (default), \"json\", \"base64\"\n  }\n}]\n</code></pre> Encoding Stored as <code>plain</code> Raw UTF-8 string <code>json</code> JSON-encoded value <code>base64</code> Base64-encoded bytes \u2014 safe for binary values"},{"location":"sinks/zookeeper/#node-types","title":"Node types","text":"<pre><code>datasources: [{\n  zookeeper: {\n    hosts:      [\"zookeeper:2181\"]\n    ephemeral:  false    // create persistent znodes (default)\n    sequential: false    // create sequential znodes\n  }\n}]\n</code></pre>"},{"location":"sinks/zookeeper/#acl","title":"ACL","text":"<pre><code>datasources: [{\n  zookeeper: {\n    hosts: [\"zookeeper:2181\"]\n    acl: {\n      scheme:         \"digest\"\n      id:             \"vartrack:hashedpassword\"\n      world_readable: true   // also grant world:anyone read permission\n    }\n  }\n}]\n</code></pre>"},{"location":"sinks/zookeeper/#drift-detection","title":"Drift detection","text":"<p>The watcher recursively lists all znodes under the configured path and compares their data (bytes) against the Git baseline. It also registers a ZooKeeper session watcher for near-real-time detection in addition to the periodic poll.</p> <pre><code>rules: [{\n  platform:   \"github\"\n  datasource: \"zookeeper\"\n  self_heal:  true\n}]\n</code></pre>"}]}